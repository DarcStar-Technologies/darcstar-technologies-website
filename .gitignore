node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
# direnv loader — dev-local; keep uncommitted so git worktrees fall through to the repo-root .envrc
.envrc

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

*storybook.log
storybook-static
# SQLite
*.db

# Claude Code local config (personal / machine-specific)
.claude/gh-user
