node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
# direnv loader — dev-local; keep uncommitted so git worktrees fall through to the repo-root .envrc
.envrc

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
# Vitest failure reports (DAR-90) — written only on a red run, kept so re-running can't destroy the
# evidence. Deliberately NOT under test-results/: Playwright clears that directory on every run.
test-failures

*storybook.log
storybook-static
# SQLite
*.db

# Claude Code local config (personal / machine-specific)
.claude/gh-user
