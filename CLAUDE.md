# CLAUDE.md

Guidance for Claude Code (claude.ai/code) in this repository.

**This file is an index, not a manual.** Keep it lean: each subsystem and durable lesson lives in a short doc under `docs/`, linked below. When you add a subsystem or learn something that isn't obvious from the code, write/update its doc and add a line here — don't inline the detail.

## Project

DarcStar Technologies marketing website — SvelteKit (Svelte 5) on **Cloudflare Pages**. Still close to a fresh `sv create` scaffold; most of `src/routes` and `src/stories` is template/demo content.

Non-negotiables:

- Package manager is **pnpm** (pinned in `packageManager`; `.npmrc` sets `engine-strict=true`). Use `pnpm`, never `npm`.
- **Svelte 5 runes only** — runes mode is forced project-wide. No legacy `export let`. See [docs/svelte.md](docs/svelte.md).

## Map

| Area                                                    | Doc                                                            |
| ------------------------------------------------------- | -------------------------------------------------------------- |
| Commands & tests                                        | [docs/commands.md](docs/commands.md)                           |
| Svelte conventions (runes, mdsvex, Svelte MCP)          | [docs/svelte.md](docs/svelte.md)                               |
| i18n — Paraglide / inlang                               | [docs/i18n.md](docs/i18n.md)                                   |
| Styling & UI — Tailwind v4, Skeleton v4                 | [docs/styling.md](docs/styling.md)                             |
| SEO — head tags & OG/Twitter social card                | [docs/seo.md](docs/seo.md)                                     |
| Contact form — modal → Turso                            | [docs/contact.md](docs/contact.md)                             |
| Waitlist — lead capture + v2 qualification flow         | [docs/waitlist.md](docs/waitlist.md)                           |
| Auth — Better Auth (server-only, locked down)           | [docs/auth.md](docs/auth.md)                                   |
| Security response headers — CSP, HSTS, etc.             | [docs/security-headers.md](docs/security-headers.md)           |
| CMS — Sanity content feed (/news · /research · /people) | [docs/sanity.md](docs/sanity.md)                               |
| Legal — /privacy · /terms pages + form notices          | [docs/legal.md](docs/legal.md)                                 |
| Evidence — /evidence claim cards backing homepage stats | [docs/evidence.md](docs/evidence.md)                           |
| Deployment — Cloudflare                                 | [docs/deployment.md](docs/deployment.md)                       |
| Skeleton LLM reference (large)                          | [docs/llms/skeleton-svelte.txt](docs/llms/skeleton-svelte.txt) |

## Lessons

Durable gotchas — keep to one line; link to the doc that carries the detail.

- Keep `@tailwindcss/forms`; Skeleton's form components depend on it. → [styling](docs/styling.md)
- Site is **dark-only**: `data-mode="dark"` hardcoded on `<html>` in `app.html`, no toggle. Distinct from `data-theme` (palette). → [styling](docs/styling.md)
- Palette is the **custom `darcstar` Skeleton theme** (`src/themes/darcstar.css`); regenerate ramps via `node scripts/gen-theme.mjs`, don't hand-edit. → [styling](docs/styling.md)
- The RGB **color-charge triad has ONE source** — the theme (`tertiary`/`secondary`/`primary` = R/G/B). `--charge-*` and `CosmicBackdrop`'s canvas both derive from `--color-*-500`; never re-type the hexes. → [styling](docs/styling.md)
- **Brand type is self-hosted** (Space Grotesk / Inter / JetBrains Mono via Fontsource `@import` in `layout.css` — CSS, not a JS import, or svelte-check chokes). Body/heading tokens live in `gen-theme.mjs`; mono/sans in `layout.css` `@theme`; bare `<h1>`–`<h6>` need the base-layer rule (Skeleton styles only `.h1`–`.h6`). → [styling](docs/styling.md)
- **Every page's hero uses one pattern**: `eyebrow` → `#helix-slot` (CosmicBackdrop centres AND SIZES the RGB helix there — the slot's **height** caps the helix amplitude, `amp = min(span*0.11, slotH*0.42)`) → heading+lede in a **`glass-card`**; never a bare centered heading. The **homepage** keeps the panel fully **below** the helix (the helix is the centrepiece). **Every other page** — the shared `PageHero` (`/news` · `/research` · `/people` + their detail pages, `/privacy` · `/terms`) and `/about` — pulls the panel **up by nearly the full slot height** (`min-[360px]:-mt-[min(23vw,17.5rem)]` — a hair less than the slot's own `min(25vw,19rem)`, leaving a small gap below the eyebrow; still can't over-pull) so the hero starts **just below the eyebrow with breathing room, its top slightly above the helix's start**; the helix becomes a backdrop behind/beside the frosted panel (its wider outer arcs peek out at the sides). Keep the `#helix-slot` height — it caps the helix amplitude, so shrinking it shrinks the helix. → [styling](docs/styling.md)
- **Keyboard focus is ONE ring plus `hover-focus:` (DAR-57)** — nothing in the repo defined a focus style, so every link fell back to the UA ring on surfaces where every other state is designed (the theme's `--anchor-text-decoration-focus` is a red herring: it only feeds Skeleton's `.anchor` **class**, which this site never uses; Skeleton leaves the global rule to the app, commented example and all). Now one `@layer base` rule outlines `a`/`button`/`summary`/`input`/`select`/`textarea` in `2px solid var(--color-primary-400)` — **every interactive element, not just anchors**, because branding the link ring and leaving buttons on the UA default trades a missing indicator for an inconsistent one. Five things worth keeping: (1) `@layer base` is what lets a surface that OWNS its focus state still win (`glass-field`'s recessed ring, `/admin`'s `focus-visible:ring-*`), making the rule **opt out only by REPLACING it**; (2) it's an explicit element list, not a bare `:focus-visible`, and the reason is the **`tabindex="-1"` containers, not `<body>`** — Kit focusing `<body>` after a client-side nav looks like the risk but was **measured to be a non-issue** (Chromium matches neither `:focus-visible` nor `:focus` on it), while the contact/login dialog panels are `div[tabindex="-1"].glass-card` and a bare selector would ring the whole frosted panel the moment the dialog library focused its content rather than the close button; (3) the ring says WHERE focus is, `hover-focus:` says the thing IS interactive — a `hover:`-only affordance tells a mouse user that and a keyboard user nothing, so the variant fires on both and its `@media (hover: hover)` guard is load-bearing (Tailwind's own `hover:` compiles with it; a raw `&:hover` would start applying hover styles on touch). It composes — `group-hover-focus:` for the news card; (4) a global rule has NO call sites, so `focus-visible.e2e.ts` is the only thing that can prove it fires: real **Tab** presses (`:focus-visible` is a heuristic under `element.focus()`), links/buttons held to the exact ring, **fields held to "looks different focused than unfocused"** (which is what makes the opt-out rule tested rather than merely documented — and note `outline: none` does NOT reset `outline-offset`, so a naive appearance comparison reports a change that renders nothing and a broken field passes), plus a **minimum stop count** so a walk finding nothing fails rather than passing vacuously; (5) Tailwind v4's `transition-colors` includes **`outline-color`**, so the ring fades in from `currentColor` over 150ms — invisible to a human, but it makes a computed-style read taken right after focusing report the element's text colour, which is why the walk waits for each stop's transitions to settle. → [styling](docs/styling.md)
- **Shared class strings live in `$lib/styles.ts`, never a component's `<script module>`** — form chrome (`fieldClass`/`submitButtonClass`/`fieldLabelClass`/`fieldHelpClass`) plus the link treatments. A route that needs only the string shouldn't have to import a form component to get it, and a control that renders its own label (GlassSelect's native fallback, step 4A's textarea) can't drift from one that delegates. → [styling](docs/styling.md)
- **Raised frosted surfaces share a `@utility glass` base**; variants (`glass-nav`/`glass-card`/`glass-btn`) `@apply glass` + deltas. A Tailwind v4 `@apply` **inlines styles but never adds the applied class name** — a `class="glass-card"` panel carries no `glass-panel` token, so `querySelectorAll('.glass-panel')` matched nothing and the sheen silently missed every panel (#108). Fix + rule: the sheen selects **structurally** — `[class*="glass-"]:not(.glass-field):not(.glass-menu)` (`glass-sheen.ts`) — but that's a **substring** match, so name new raised surfaces `glass-*` and don't reuse `glass-` in a non-raised token (or extend the `:not(...)`). → [styling](docs/styling.md)
- `pnpm preview` runs the built worker on the real Workers runtime (Wrangler), not `vite preview`. → [commands](docs/commands.md)
- **The preview port is derived per checkout, and e2e never reuses a running server (DAR-79)** — `pnpm preview` and `playwright.config.ts` both read `scripts/preview-port.mjs` (main checkout `4173`; each linked worktree a stable slot in 4174–4272, hashed from its path; `PREVIEW_PORT` overrides, and a non-port value is refused rather than ignored). They used to hardcode 4173 each, and Playwright's `reuseExistingServer` defaults **on** locally — so a preview running in a **sibling worktree** answered the whole suite and it came back green for someone else's branch. Reuse is off now (`E2E_REUSE_SERVER=1` opts back in, warning that the results describe whatever is listening), and a busy port names its owner's **pid + cwd**, because the reflex fix is a `pkill` that kills another session's server. Three mechanics worth keeping: the config is re-imported in **every Playwright worker** — by then our own webServer holds the port, so any port check must be gated on `TEST_WORKER_INDEX === undefined` or the suite fails against itself; `workerd` is wrangler's **grandchild**, so kill the **tree root** (killing the socket owner just makes wrangler respawn it) and a SIGKILLed parent leaves it holding the port under `ppid 1` forever — the source of the stale servers, which `preview.mjs` now reaps three ways (forward SIGTERM, SIGKILL a wedged tree, and ask the port whether something outlived the child, since leftovers stop being its descendants the moment it dies). **Finding that tree root by "walk up while the ancestor is node" walks straight OUT of the tree into whatever supervises it** — under vitest that's the test runner, and the reaper SIGTERMed it (a mutation test proved it: `Terminated`, exit 143); the stop condition must be conjunctive, argv[0] **and** a preview-shaped command line; and CI is a main checkout (`.git` is a **directory**, a linked worktree's is a **file**), which is what keeps `test.yml`'s hand-written `ORIGIN=http://localhost:4173` true — pinned by a spec, since one half is YAML. → [commands](docs/commands.md)
- Run `pnpm gen` after changing `wrangler.jsonc` bindings **or `.env.example`** to refresh the `Env` type — generation is **deterministic** (DAR-49): env-var names come from `.env.example` (never `.env`), volatile wrangler output is normalized, and the `check` CI job drift-guards the committed `worker-configuration.d.ts`. Never run `wrangler types` directly. → [deployment](docs/deployment.md)
- **Prod and dev are SEPARATE Turso DBs, one per Cloudflare Worker.** Prod Worker (`main`→darcstar.tech) → prod DB; a **separate preview Worker** (`[env.preview]` in `wrangler.jsonc`, non-prod branches, its OWN Workers Builds trigger running `wrangler versions upload --env preview`) → dev DB; local `.env` → dev DB. Each Worker holds its own `DATABASE_*` secret, so `getDb()` has **no** env logic — isolation is at the secret layer. Only the **bare** `…-preview.…workers.dev` host mounts `/api/auth/*` (ORIGIN is one value); per-branch preview URLs 404 there. First-time split runbook + the preview Worker's own secrets are in [deployment](docs/deployment.md#environments--databases). → [deployment](docs/deployment.md)
- **A Workers Build deploys the Worker it is CONNECTED to, whatever the config says — so an `[env.*]` block is inert without a trigger of its own (DAR-131)** — every build passes `WRANGLER_CI_OVERRIDE_NAME`, and wrangler obeys it (`Failed to match Worker name … Overriding using the CI provided Worker name`), so the prod-connected build ran `--env preview` while uploading versions of **production**: preview `vars` applied, production **secrets** retained, every feature-branch preview URL reading and writing the **production** database. Five things worth keeping: (1) the ticket's diagnosis was that the deploy command "was never changed from the default" and it was **already** `--env preview` — the flag was never the gate, so the fix is a second build trigger bound to the preview script plus deleting the prod Worker's non-prod trigger, and the runbook's "or let Workers Builds create the Worker" was false for the same reason (a connected build cannot create a differently-named one); (2) the ticket also read a `302` on a preview URL as DAR-81's ORIGIN mismatch, and it was **Cloudflare Access** — an app on `*-darcstar-technologies-website.darcstar.workers.dev` — which both narrowed the hazard (the prod-DB previews were login-gated, not open) and, because the preview Worker's hosts end `-website-preview.` and miss that pattern, is why the provisioned Worker is publicly reachable; **measure the redirect, don't infer the cause from the status**; (3) `versions upload` never shifts traffic, so production itself was never at risk — the damage was confined to what those URLs could read and write; (4) only the **bare** preview host mounts `/api/auth/*`, since `ORIGIN` is a single value matched against the request origin, so per-branch URLs are for looking at the site and the bare host is the auth/limiter probe target — its DB-backed limiter binding (a 429 with `x-retry-after: 3600` on the 4th sign-up POST) is what proves the dev DB is live, a stronger check than any page rendering; (5) "is this Worker deployed?" is not a question a test can ask, so the checkable half is the repo's: the Worker names live once in `auth-options.ts` (with `trustedOrigins` derived from them — the preview wildcard is NOT covered by the prod one, a near-miss that reads as covered), and `preview-worker.spec.ts` pins them against `wrangler.jsonc`, **`[env.preview]`'s existence asserted first** so the ORIGIN and `assets` checks can't pass vacuously against the very state DAR-131 found. Its comment stripper is string-aware because every value it checks is a URL and a naive `//` strip eats `https://`. → [deployment](docs/deployment.md)
- Changing the DB schema? Run `pnpm db:generate` + commit the `drizzle/` migration — the **`drizzle` CI check** (+ a pre-commit hook) regenerates offline and fails on drift, gating merge→prod. **Dev DB** applies via `pnpm db:push`; **prod DB** applies via the **`deploy-prod` GitHub Action** which `pnpm db:migrate`s **then** `wrangler deploy`s on push to `main` (`PROD_DATABASE_*` + `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` secrets), so a failed migration **blocks the deploy** (migrate-before-deploy; Workers Builds does previews only, prod auto-deploy disabled) — never `db:push` prod. An existing `db:push` prod DB needs a one-time `pnpm db:baseline` so migrate is forward-only (skips replaying `0000`). → [deployment](docs/deployment.md)
- `checkJs` is **off** — generated Paraglide `.js` clashes with the Cloudflare `Request` global; no hand-written `.js` exists, so `.js` bodies aren't checked. → [i18n](docs/i18n.md)
- **All UI copy lives in Paraglide messages** (`messages/*.json`); the `local/no-raw-text` ESLint rule blocks hardcoded `.svelte` text/attrs (Wordmark exempt). `es` is untranslated → non-base locales are `noindex` via `Seo.svelte`'s `TRANSLATED_LOCALES` flag; hreflang deferred until `es` is real. → [i18n](docs/i18n.md)
- **A non-base catalog holds ONLY translated keys (DAR-53)** — `es.json` was a byte-identical copy of `en.json`, so Paraglide compiled two identical bodies per message; omitting a key compiles the base-locale fallback as a free alias (`const es_x = en_x`) with identical output (−39 KB raw / −6.8% of the client bundle, but only −172 B gzipped — the win is parse+cache, not wire). The convention was documented before and drifted anyway, so it's now `message-catalogs.spec.ts`: **never re-add a key equal to the base value** (true even when the translation legitimately matches — omitting renders the same text for less) and **never add a key the base lacks** (Paraglide compiles an orphan's _base_ variant as the quoted key name, so a typo silently no-ops the translation while English still looks right). Nothing warns — there is no message-lint module in `project.inlang/settings.json`. The spec derives its catalogs by glob, but `evidence-boundary`/`safety-language` scan `en`+`es` **by name**, so it also pins the locale set. → [i18n](docs/i18n.md)
- e2e builds + previews the Cloudflare bundle; match the vitest project to your filename (`client` / `server` / `storybook`). **Six required CI checks gate merge→main** (lint, commitlint, migrations in sync, check, unit tests, e2e — DAR-49); both test suites are **hermetic** (no `.env` in CI), keep new tests that way. → [commands](docs/commands.md)
- **A red unit run records itself — don't keep the log (DAR-90)** — one run reported `1 failed | 590 passed` through a `grep` pipe and the reflex re-run destroyed the only copy of the name, so the evidence moved into the machine: a reporter (`scripts/vitest-failure-report.ts`, wired at the **root** `test.reporters` — not a per-project option) writes `test-failures/<ISO>-<pid>.json` on a red run and **nothing** on a green one, so anything in that directory is by construction a failure. Four load-bearing properties: the filename is **unique per run**, because the built-in `json`/`junit` reporters take a FIXED `outputFile` and would re-create the exact "re-running destroys it" defect one directory further from the terminal; a **collection error is recorded even with zero failed test cases** (a module throwing at import makes a red run whose `allTests('failed')` is empty — a reporter walking only failed cases writes "no failures" for it, which misleads worse than silence); the report carries the **10 slowest tests of the run**, since `loadavg` alone can't separate "the box was busy" from "the test is broken" but a baseline can; and every entry point is **fail-soft**, because a reporter that throws turns a green run red. One trap: a CLI `--reporter=…` **replaces** the configured list (so `--reporter=verbose`, the flag you'd type while chasing a flake, silently switches recording off, and `--reporter=default` does NOT restore it) — name the module itself as a second `--reporter`, which is what its default export is for. Conformance to vitest's classes is structural — the builder takes minimal interfaces so the spec needs no casts, and the reporter's typed `onTestRunEnd` passing real `TestModule`s is what makes an API drift a `pnpm check` error (mutation-proven). Measuring also **refuted DAR-90's own leading theory**: `preview-port.spec.ts`'s `descendants` runs in **121 ms**, nowhere near its timeout, while the tightest margin is the **browser** projects — the same test measured **914 ms and 2346 ms on one machine** (2.6× run-to-run variance, 2.1× margin against the 5 s default at the slow end), which is why `client`/`storybook` set `testTimeout: 15_000` and `server` keeps the default. → [commands](docs/commands.md)
- **The preview overrides the env that can't work on localhost, and TWO gates hid the auth API behind it (DAR-81)** — `/api/auth/*` was untestable in e2e, and the reason it stayed that way is that both gates fail **closed into something that reads like a pass**: better-auth's `isAuthPath()` mounts the API only for requests whose origin matches `baseURL`, so a mismatched `ORIGIN` yields a **404 from Kit's router**, and the rate limiter runs ahead of every auth route with its counters in the **database**, so this suite's placeholder `DATABASE_URL` yielded a **500** — and `expect(res.ok()).toBe(false)` is satisfied by both, which is exactly how DAR-67's boundary shipped with a test that would have stayed green if registration were re-opened. Fixed by baking three `--var`s derived in one place (`scripts/preview-vars.mjs`): Turnstile test keys, `ORIGIN:http://localhost:<port>`, `AUTH_RATE_LIMIT_STORAGE:memory`. Six things worth keeping: (1) a **`--var` beats a `.env` entry** (measured), which is the whole reason this belongs in the preview script — CI hand-wrote `ORIGIN=http://localhost:4173` into `.env` and so reached the auth API, while a local run (whose `.env` holds the **dev server's** origin, on a port a worktree derives independently) silently tested a `/api/auth` that 404'd, i.e. **one suite testing two different things**, DAR-79's defect in a new place; (2) deriving ORIGIN from the port **deletes** the old `preview-port.spec.ts` YAML pin rather than updating it — the two copies it guarded no longer exist; (3) the limiter's store is the **one env-bound thing** in it and lives in `auth.ts`, not the shared `auth-options.ts` (which is deliberately env-free), with **fail-safe polarity** — only the literal `memory` switches it, `wrangler.jsonc` never declares the var, because memory storage is per-isolate = no limiter at all on Cloudflare; (4) the limiter is **real in a preview**, so `E2E_REUSE_SERVER=1` keeps its counters and the 4th run in an hour trips the 3/hour sign-up cap — the spec says so in its failure message; (5) **nothing downstream needs the DB** — with the limiter in memory a dead database still gives `400 EMAIL_PASSWORD_SIGN_UP_DISABLED` and `GET /ok` → 200, which is what makes a hermetic assertion possible at all (measured; the transaction-opens-first theory was refuted); (6) the mount is asserted **separately and first**, so a harness regression reports itself as a broken harness instead of as a boundary that still holds. → [commands](docs/commands.md) · [auth](docs/auth.md)
- Use the Svelte MCP (list-sections → get-documentation) for Svelte/SvelteKit questions; run svelte-autofixer on any Svelte you write. → [svelte](docs/svelte.md)
- Page head (title/description/OG/Twitter) comes from one `<Seo>` component — render it **once per page** in `+page.svelte`, never the layout (SvelteKit merges heads → duplicate tags). OG card is `node scripts/gen-og.mjs` (Chromium rasterizes the brand to 1200×630 PNG). → [seo](docs/seo.md)
- **SEO discovery surfaces (DAR-48)** — `/sitemap.xml` is a worker route (static `STATIC_PATHS` + Sanity slugs; **new indexable route → add it there + to the security-headers `AUDITED_PAGES`** — a route-enumerating e2e fails loudly if the sitemap is forgotten); robots.txt hardcodes the prod sitemap URL (static asset). JSON-LD: pure builders in `$lib/jsonld.ts` — **keep it dependency-pure: the root layout imports it (site-wide bundle)**, so pages pre-resolve image URLs via `imageUrl` (`$lib/sanity/image.ts`); **Organization ships from the root layout** (deliberate — not OG tags), page nodes via `<Seo jsonLd>`; the "locale is real" flag `TRANSLATED_LOCALES` lives in `$lib/seo.ts`, shared by Seo + sitemap + e2e. → [seo](docs/seo.md)
- **A CMS page's head is one mapper, and "hide from search engines" is TWO surfaces (DAR-71)** — the detail pages don't hand-map Sanity's `seo` object onto `<Seo>` props; they spread `contentSeo(doc.seo, fallbacks)` (`$lib/sanity/content-seo.ts`), because the per-page copy-paste is exactly how `noIndex` came to be fetched, typed, and **dropped at both render sites** (new `seo` field → wire it there, never at a call site). A Svelte spread **does not reject unknown props**, so the mapper returns `Pick<SeoProps, …>` against `Seo.svelte`'s exported props to make a _misnamed_ key a compile error; an _omitted_ key still type-checks (every prop is optional) and is caught only by `content-seo.spec.ts` — both guards are load-bearing. Honoring the toggle needs the robots meta **and** a `seo.noIndex != true` filter on `sitemapEntriesQuery` — noindex alone still advertises the page to crawlers. Polarity is **fail-open** in both (`=== true` / `!= true`, never `== false`): hiding needs a POSITIVE signal, and since no document sets `seo` at all, the inverse would de-index the site in one deploy. The rule can't be single-sourced (one half is GROQ), so it's pinned twice — `content-seo.spec.ts` + `queries.spec.ts`. Hidden ≠ unpublished: the page stays live and listed on `/news` · `/research`. → [seo](docs/seo.md)
- **`canonical` and `og:url` are NOT the same value (DAR-70)** — they were one derived expression in `Seo.svelte` until third-party paper pages needed to canonicalize to arXiv/the publisher (they quote the source abstract verbatim → duplicate content). Only `<link rel="canonical">` is overridable; **`og:url` always stays this page's own URL**, or every social share of our page would carry arXiv as its graph identity. Precedence: editor's `seo.canonicalUrl` → the page's derived `paperSourceUrls(paper)[0]` (`$lib/jsonld.ts`, ordered `url` → DOI → arXiv, **shared with the ScholarlyArticle `sameAs`** so the doi.org/arxiv.org templates exist once) → self. **Both** inputs pass the same `isHttpUrl` gate (absolute http(s), no whitespace — `new URL` turns a prose-filled `doi` into a well-formed 404, and the Studio's own url validation is a UI affordance an API write skips); an unusable override falls THROUGH to the derivation. First-party (`darcstarAuthored === true`) stays self-canonical, fail-safe like DAR-52. Canonicalized pages **stay in the sitemap**; JSON-LD `mainEntityOfPage` stays our page. → [seo](docs/seo.md)
- Contact CTAs open **one** modal (`ContactDialog`, rendered in the layout) via the `contactDialog` rune; it submits through a SvelteKit **remote `form`** (`contact.remote.ts`) → `contact_submission` in Turso, with a honeypot + IP/time throttle, then **two fire-and-forget Resend emails** (`contact-notify.ts`, via `ctx.waitUntil` — never fails the submission): a lead → `info@` and a localized **submitter ack** (Paraglide `contact_ack_*`, RFC-3834 auto-reply headers, `allSettled` so a bounced ack never drops the lead). Needs `RESEND_API_KEY` + a verified Resend domain. `.remote.ts` may live anywhere except `$lib/server`. Run `pnpm db:push` after schema changes (the default apply path; `pnpm db:generate`+`db:migrate` write/apply a versioned `drizzle/` trail when wanted — see [deployment](docs/deployment.md)). → [contact](docs/contact.md)
- **Waitlist v2 data model (DAR-59)** — `/waitlist` keeps its v1 remote-`form` (honeypot + IP throttle + `isNew`-gated emails + anti-enumeration), now with qualification columns for the progressive flow (DAR-58). The optional steps 2–4 are **unauthenticated enrich writes** authorized by a **signed continuation token** (`waitlist-token.ts`, HMAC-SHA-256 over reused `BETTER_AUTH_SECRET`, domain-separated, 24h, **canonical** — one string per id/exp) returned in step 1's response; because the same success shape (token included) is returned for new **and** existing email (anti-enumeration), the token reaches any submitter of a known address, so `applyWaitlistStep` guards with an **explicit per-step column map** (mass-assignment guard — never identity, never another step's answers) and per-field keep-existing. `contact_permission` is **tri-state** (null = not shown / false = declined / true = granted; validator gates on `isPositivePilotInterest`, store keep-existings null so a not-shown submit can't revoke). `consent_updates` is an **unverified single-opt-in claim** (monotonic, `consent_updates_at` provenance) — must **not** drive a send without double-opt-in. Multi-select step fields **must** be named `foo[]` (SvelteKit's form-data conversion throws on a repeated plain name). `role` holds both v1 + v2 slug sets — canonicalize before branching (`canonicalizeWaitlistRole`, DAR-62). → [waitlist](docs/waitlist.md)
- **Waitlist v2 step-3 gate (DAR-62)** — which step comes next is decided **server-side** and shipped in the step response (`next`); the rule lives ONCE in `$lib/server/waitlist-flow.ts` (DAR-63's step-4 fork + DAR-65's classifier reuse it — never restate it), and living under `$lib/server` makes "not client-authoritative" structural rather than a convention. `isCommercialUseCase` = DAR-62's exclusions (researcher/student/investor-advisor, research-education) **plus fail-safe polarity** — commercial needs a POSITIVE signal, so unanswered/unrecognized routes PAST the money questions (matches the epic's "incl. unanswered → branch B"). The gate is **UX, not authorization**: a crafted POST straight to step 3 still validates + stores, because answering buys no privilege. Two mechanics every later step inherits — each response **echoes the submitted token** (verbatim, capped, never re-minted) so the next step's hidden field survives a no-JS re-render, and the enrich is **best-effort** (`applyStepBestEffort` logs a DB failure instead of erroring the visitor's flow — the fire-and-forget-email posture — and **skips the write for a honeypot decoy id**, so trap-tripping bots cost no DB writes and the step e2e stays DB-free). → [waitlist](docs/waitlist.md)
- **Waitlist v2 step-4 fork (DAR-63)** — the flow ends in one of two branches: **A** (active commercial interest → pilot question, and only for a POSITIVE answer a revealed block of deployment-scale free text / contact permission / method / phone) or **B** (research-general → an uncapped "what to send you" multi-select, never money or pilots). The fork reads `evaluation_timeline` **alone** (`step4BranchFor`, near-term → A, everything else incl. unanswered → B) — role gates step 3, not this. Two rules worth keeping: (1) step 3 does **NOT** read the stored row to re-derive the branch — `next` deriving from stored state would be an **enumeration oracle** (the continuation token reaches any submitter of a known email, so `next: 'step4a'` would prove that address is on the list with a near-term timeline); instead step 2 **signs** the branch (`mintWaitlistFlowClaim`, own domain + `f1` prefix on the shared `mintSignedValue` core; DAR-64 widened its payload to carry the CTA audience too) and step 3's form carries it as a hidden field, so it's server-decided AND untamperable. (2) The conditional reveals are **`mounted`-gated `{#if}`s — enhancement, never a gate**: no-JS renders every field, and the server independently emits `contact_permission: null` ("never asked") unless the pilot answer is positive. → [waitlist](docs/waitlist.md)
- **Waitlist v2 confirmation (DAR-64)** — every path ends on `WaitlistConfirmation.svelte` with **one** CTA, and **which** one is a server decision (`confirmationCtaFor`, ordered strongest-signal-first: positive pilot → `/contact`, commercial → `/evidence`, research/investor → `/research`, unknown → `/`, the fail-safe floor). The step-2 decisions that feed it ride in the **signed flow claim** — DAR-63's branch claim generalized to payload `<branch>|<audience>` under a new domain/`f1` prefix, minted once at step 2 and echoed verbatim through steps 3–4 (one claim, not one per fact) — because deriving them from the stored row would be the same enumeration oracle DAR-63 rejected. Two rules: the confirmation is **fixed copy plus a link**, never a summary (internal-only budget/impact/deployment answers have no path there — the e2e fails on a currency figure or those labels), and the pilot CTA is a **real `/contact` anchor** that JS upgrades into the modal (modifier-clicks untouched, `aria-haspopup` `mounted`-gated). The epic's `evaluation_conversation_requested` event is **not fired** — no analytics transport exists; the call site is marked for DAR-66. → [waitlist](docs/waitlist.md)
- **Waitlist v2 lead classification (DAR-65)** — `classifyWaitlistLead` (`$lib/server/waitlist-classify.ts`) buckets a signup into `priority-a|b|c` / `research` / `investor` for `/admin/waitlist`, **computed on read, never stored** (every input is an already-persisted column, so a denormalized copy would only add a migration + a recompute obligation per step write). Four rules: (1) the **money guardrail is the input type's shape** — `economic_impact`/`budget_range` are absent from `WaitlistLeadSignals`, so a self-reported figure _cannot_ be scored (a concrete $25k prospect outranking an anonymous ">$1M" is unit-pinned), and the figures stay on the row detail for human judgement; (2) it **reuses** `audienceFor`/`canonicalizeWaitlistRole`/`isActiveEvaluationTimeline` rather than restating them, or the rubric would judge people by questions they were never asked — and inherits their fail-safe polarity (an empty row is `research`, never a priority band); (3) **internal only** — `$lib/server` makes "no public page can import it" structural, and only the slug vocabulary `WAITLIST_LEAD_CLASSES` is client-safe (in **triage order, so its index IS the sort rank**) because the badge needs a label, exactly DAR-64's CTA split; (4) the epic's optional **Priority-A notification was deliberately NOT built here** — built later as DAR-82, once DAR-88 removed its blocker. The admin view sorts priority-first (stable sort keeps newest-first within a band), filters via a no-JS `?class=` GET with whole-window counts, renders `contact_permission` as the **tri-state** it is, and prints the "internal guess, unverified claims" caveats under the table. → [waitlist](docs/waitlist.md)
- **Waitlist v2 Priority-A notification (DAR-82)** — one email into `info@` the first time any of a lead's submissions classifies Priority A, and the interesting part is that **DAR-65's stated blocker had already been dissolved by DAR-88** (the ticket was filed hours before it merged and still describes it as live): the mailbomb shape was "the continuation token reaches any submitter of a known address", and append-only means a token now addresses the submission its own holder created, so nobody can push another person's row into a band. Six things worth keeping: (1) the cap **IS a column, not a counter** — `UPDATE waitlist_lead SET priority_a_notified_at = … WHERE priority_a_notified_at IS NULL RETURNING id`, so one row back means this call and no other claimed it (DAR-66's composite-key move, and `isNew`'s: the DB decides inside the statement that does the work, so five concurrent step writes send one email with no counting query); (2) **per LEAD, never global** — a shared bucket would be a **denial-of-notification** primitive (flood it and the real Priority-A lead goes unannounced), whereas per-lead means one person's abuse spends one person's budget, with step 1's per-IP throttle bounding how many distinct leads a source can mint; (3) **claim BEFORE send, the opposite polarity to DAR-67's invite** — that one mails first so a failure stays retryable because an operator is standing over it, while this fires from a visitor's step submit that will never happen again, so at-most-once is the property worth buying and its cost is one lost email on a lead that is still sorted to the top of `/admin/waitlist`; the Resend key is therefore checked **before** the claim, or a keyless deploy burns every lead's one notification on sends that never happen; (4) it hangs off **`applyStepBestEffort`**, the single chokepoint, and fires on **every step rather than only 4A** — it's the combination that scores and provided-wins means a walked-back step 2 can supply the last piece, so a step-number gate would be true today and silently wrong after the next reload-resume change; (5) **fire-and-forget includes the CLAIM** — both it and the send run inside `ctx.waitUntil`, because an awaited conditional UPDATE would add a round trip on exactly the submits that classify A and whether it matched a row answers "has this address been flagged before?", i.e. a timing oracle over state the visitor can't see; (6) the email names the four rubric inputs and says **invite them** with a link to `/admin/waitlist?class=priority-a` (DAR-67 invites from that page, so "a hot lead arrived" and "someone should invite them" are one moment) — with **no money figure, structurally**, since the row list is a `Record<keyof WaitlistLeadSignals, …>` and DAR-65 left `economic_impact`/`budget_range` out of that interface, and with the link built from **`ORIGIN`, never `url.origin`** (Host is forgeable, and this URL lands in mail we send ourselves and act on). → [waitlist](docs/waitlist.md)
- **Waitlist v2 funnel analytics (DAR-66)** — the site's first analytics, first-party by construction: a `waitlist_funnel_event` row is `(flow_id, event, created_at)` and **nothing else**, so there is no column an IP/UA/email/answer could go in (the DAR-58 internal-only free text has nowhere to land) — pinned by a unit spec on the column list, the same "make it structural" move DAR-65 used to keep dollar figures out of the rubric. Five rules: (1) the **per-flow cap IS the composite primary key** — `(flow_id, event)` + `onConflictDoNothing()` bounds a flow to one row per event in the insert itself (no counting query), so a replay/double-click/bot re-POST is a no-op AND every count is a count of **distinct flows**, which is what makes `waitlist_signup_completed / waitlist_viewed` a conversion rate rather than a ratio of retries; (2) `waitlist_viewed` rides the `/waitlist` load on a **GET** rather than a `sendBeacon` endpoint (no second public write surface, no CSP origin, and the no-JS visitor is counted) — guarded on `request.method`, because Kit re-runs loads when it re-renders after a native step POST (`render_page` → `handle_remote_form_post`) and counting those would inflate the denominator by one view per step; (3) the **flow id is random, never derived** from the row id or email (a derived id would be joinable back to `waitlist`), shape-checked on write, and **echoed verbatim by every step** exactly like the continuation token — without JS each POST re-renders and its load mints a fresh id, so an unechoed handle splits one visitor across four flows; (4) the one client-fired event (`evaluation_conversation_requested`, the only funnel moment with no server request of its own) goes through a remote **`command`** bounded by `CLIENT_FIREABLE_FUNNEL_EVENTS`, **not** the full vocabulary — the `applyWaitlistStep` mass-assignment guard again, since POSTing `qualification_completed` would inflate the very numbers this reports; (5) `captureWaitlistFunnel` returns **void** (nothing can await analytics) and the admin readout — the one awaited query — is **fail-soft** to an "unavailable" note, because a deploy landing before its migration must not take the triage list down. Sixth, non-obvious: **a link to /waitlist must opt out of hover prefetch** (`data-sveltekit-preload-data="tap"` on the homepage CTA + the footer link) — `app.html` sets `hover` body-wide and preloading DATA runs the load, so every mouse pass over a link would count a view for a page nobody opened; `tap` still prefetches on pointerdown and a click reuses that one request, and an e2e asserts it at the network layer. The honeypot records no SIGNUP event (bots stay out of the conversion metric); **DAR-83** extended that to the rest of the flow. → [waitlist](docs/waitlist.md)
- **The honeypot's effect must be uniform across every surface it can reach (DAR-83)** — the trap withheld the signup event but not the later stages, because steps 2–4 are reached with a **decoy continuation token** and only the DB write consulted it; a bot that tripped the trap and kept going could therefore push `qualification_started` above `waitlist_signup_completed`, an impossible sequence that reads as a bug in the readout (**our own hermetic e2e drives exactly that path**, so a run against a reachable DB was a source of it). The step endpoints now capture through `captureWaitlistStepFunnel`, which drops everything for a decoy id. Four things worth keeping: (1) the gate is a **string comparison, and the cost DAR-66 priced it at was already sunk** — it said "an HMAC per step", but every step already verifies the token before it can enrich and DAR-75's resume cookie needs the resolved id too; (2) **decoy only, never a null id** — absent/malformed/expired/tampered/no-secret all arrive as `null` and none is evidence of a bot (the visitor whose token aged out mid-flow did reach that stage, and gating on validity would take the step funnel dark on a deploy missing `BETTER_AUTH_SECRET` instead of merely not enriching), so the polarity is the mirror of the write's: suppress on a POSITIVE bot signal, and a decoy is one because it exists only for someone who filled a field no human can see; (3) **this is suppressible where the token and the resume cookie are NOT** — the insert is fire-and-forget in `ctx.waitUntil` and the counts sit behind `/admin`, so an unwritten row is invisible to the caller, whereas those two are minted precisely because their absence would be a detectable response difference; (4) the gate **can't live in `applyStepBestEffort`** (a Skip or an all-blank Continue records events without reaching it) and no type can force four call sites through it, so `waitlist-funnel.spec.ts` **reads source** and pins the **import** (a call site can't exist without the binding, and unlike matching the call text it can't be tripped by a comment naming the ungated function — namespace imports blocked too) plus ≥1 gated call per exported step form, so a fifth step can't quietly under-report — **only if it's in the file being read**, which is what **DAR-102** fixed. The `/waitlist` load's `waitlist_viewed` needs no gate: the first view precedes the trap, and a resumed decoy keeps its flow id so the re-record collides with its own first GET. One inversion remains, deliberately — `evaluation_conversation_requested` from a decoy flow, since DAR-75 drops the row id at `done` so the confirmation's command cannot know; the other (an unauthenticated POST straight at a step with a self-minted flow id) closed with **DAR-86**. → [waitlist](docs/waitlist.md)
- **A source-scanning spec is only as wide as the set it scans, and an ALLOWLIST is the shape that stays wide (DAR-102)** — DAR-83's gate spec read one hardcoded path, so "a fifth step can't quietly under-report" held only for a fifth step written into **that same file**; one added as its own module could import the ungated `captureWaitlistFunnel`, skip the honeypot gate, and pass every assertion because none of them looked at it (measured both directions — the old spec stayed **48/48 green** with exactly that file in the tree, the new one fails 2). Four things worth keeping: (1) the rule is an **allowlist over a derived surface**, not a classifier — "a file that verifies a continuation token must gate" was the alternative and is weaker in a way that matters, since extracting `resolveStepRow` into a helper leaves the new step endpoint importing `verifyWaitlistToken` **nowhere**, whereas an allowlist doesn't care how a file came by its row id and fails closed for any new one; the three exemptions (step 1, the page load, the client command) each carry their reason, so the "obvious tightening is wrong" objection is answered by naming them rather than by not having a rule; (2) a hand-written **allowlist** is fine where a hand-written **scan list** is not, and the difference is **polarity** — deleting a scan entry makes the scan blind (DAR-99 measured that at 7/7 passing), deleting an allowlist entry makes the rule _stricter_, so both edit directions report themselves; a paired assertion covers the rot direction too (an entry whose file stopped importing the ungated function fails, so the list can't become names nobody checks — same move `safety-language.spec.ts` makes for its one allowlisted key); (3) an import pin has **four** ways of being walked past and needs all four — **alias** (`{ captureWaitlistFunnel as record }`), **namespace** (`import * as`), **re-export** (`export {…} from` / `export *`, a one-module laundering path) and **relative specifier** (a neighbour's `'./waitlist-funnel'`), each mutation-proven; and the specifier half must **resolve against the importing file's directory rather than string-match**, because there are TWO `waitlist-funnel` modules (gated server / client vocabulary) so `'./waitlist-funnel'` names a different file from `$lib` than from `$lib/server` — string-matching it reports a legal namespace import of the CLIENT module as reaching the server one, a **false failure**, the one error direction a scan of this kind normally can't produce; (4) **the scope of a source-scanning rule is set by where its exception legitimately lives, and getting that wrong reproduces the defect one level down** — the first cut scoped the scan to the waitlist's three directories with a non-recursive `readdirSync`, and a step planted at `src/routes/waitlist/step5/+page.server.ts` passed **56/56**, with one under `src/routes/api/` never looked at either. "Who imports the ungated function?" has no directory boundary, so it scans **all of `src`**; DAR-99's cast-route and caller checks went app-wide with it; only "the env key is named in exactly one file" stays waitlist-scoped, because `auth.ts` names it legitimately. Shared implementation in `source-scan.ts` (one reader/stripper/import-parser, two surfaces). (5) **an allowlist must exempt CALL SITES, not files** — measured: a fifth step appended INSIDE `waitlist.remote.ts` inherited that file's pass and was invisible unless it happened to follow the `submitWaitlistStep` naming convention, so each entry now declares how many ungated captures its reason covers (one, all three), two-sided so removing the one also fails. Honest residual, now narrower than the gate: every route to the ungated function is closed, but **firing nothing at all** is still only convention-deep — an unconventionally-named step that captures no event under-reports, which is a missing number rather than a wrong one. → [waitlist](docs/waitlist.md)
- **The funnel's flow id is signed, so an analytics row costs a page view (DAR-86)** — `flow_id` travelled bare in a hidden field, so DAR-66's `(flow_id, event)` composite key capped a flow **the caller chose**: a fresh `crypto.randomUUID()` per POST defeated it outright, and the step endpoints and the public command reached the insert with **no continuation token at all** (a bare POST at step 2 wrote rows for free). It now travels as `n1.<uuid>.<exp>.<mac>` — a fourth value on the shared `mintSignedValue` core, minted **only** by `/waitlist`'s load — so the floor becomes the one `waitlist_viewed`'s own GET has always had, i.e. the one DAR-66 accepted as irreducible without a captcha. Five things worth keeping: (1) the **column still holds the bare UUID** — transport-only signing means no migration, every stored row and count unchanged, and handles in flight across the deploy merely stop counting; (2) the two forms are **different TYPES** (`WaitlistFlowId` is branded), so every request crosses from wire value to vouched-for id exactly once at `resolveWaitlistFlowId` and a call site that skipped it **wouldn't compile** — with `isWaitlistFlowId` inside the capture as the runtime half, which is why forgetting fails **closed** even under a cast (a handle isn't UUID-shaped, so it records nothing rather than filling the column with wire text); (3) the **resume cookie carries the BARE id, and that's forced** — the signing core splits on `.`, so a signed value cannot be a field inside another signed value, which is what settles the whole shape (the steps need the bare id for the cookie anyway → the steps verify → the capture takes something already vouched for). The first cut had the capture verifying and the cookie carrying the handle; **the resume spec caught it**, six tests red on a payload that no longer parsed; (4) **no secret → no funnel, uniformly** — a deploy without `BETTER_AUTH_SECRET` can't mint either, so every stage goes dark together instead of leaving `waitlist_viewed` climbing against zero conversions, a readout that misleads worse than an absent one; (5) the cap is on the **flow, not the handle** — two signed strings for one id still collapse to one row per event, so re-signing buys nothing. Rejected (the ticket's own alternative): gating the capture on the step write succeeding, which would let DAR-68's budget bound it transitively but **destroys the metric** — `qualification_started` fires for a Skip, which writes nothing, and skip-only flows are the drop-off the funnel exists to measure. → [waitlist](docs/waitlist.md)
- **A TTL belongs on a capability, and the funnel handle is not one (DAR-98)** — DAR-86 gave the handle the 24h its three siblings share, and that inherited symmetry was the whole bug: the token authorizes a write, the claim carries a routing decision, the resume cookie re-mints both, while a handle authorizes **nothing** — its bound is the composite key, which is absolute and permanent (one row per event however long it lives), so a shorter life never lowered the ceiling, it only moved when the rows could land against an all-time unfiltered readout. What it cost was the one thing the handle is for, and **twice over**: a step from a day-old tab resolved to null, so the remaining stages recorded nothing AND the null went into the resume cookie, after which every render minted a fresh flow and wrote another `waitlist_viewed` — the load reads that cookie and never writes one, so nothing repaired it, and each step re-issued it for another 24h. That is **DAR-75's `__data.json` over-count resurrected** (numerator down, denominator up), and the fix is a year-long TTL of its own, because the principle was already settled by **DAR-83** keeping the step funnel recording for a visitor whose _token_ aged out: **measurement must not be gated on authorization** — a day-old tab must fail to enrich (the capability expired) while still being counted (the visitor is still that visitor). Still an expiry, not none: the signing core requires one, a never-expires mode would change code three other values depend on, and a value that can't age out is a permanent bearer artifact — and nothing on the visitor's DEVICE lives longer either, the resume cookie being untouched at 24h. Three rejected, each for a reason worth keeping: a **cookie write inside the load** (DAR-75 kept it read-only because `<body>`'s `preload-data="hover"` runs it on mouse-over — the `?restart` trap next door); **accepting expired handles for the cookie only**, a second verification mode beside DAR-86's one crossing bought for a property just shown to be worthless, and a half-fix besides (the step carrying it still records nothing); and **suppressing the view for a resumed visitor with no flow id**, which trades denominator inflation for step events on a view-less flow, counting one visitor twice in the numerator. The spec states it **behaviourally** — at 25h the token verifies to null while the handle still yields its id — because a constant comparison passes against a build that hardcodes the TTL at the mint call and never reads the constant (mutation-proven, three tests red vs none). **And "longer than the token" is NOT the property to pin** — the first cut asserted exactly that and a mutation to a **2-day** TTL, which reinstates this very bug for a three-day tab, passed all 47 tests: the day-old test clears any TTL over ~25h and the boundary test derives its clock FROM the constant, so both self-adjust. The magnitude needs its own assertion, phrased as the claim the design makes (a tab left open for **months** is still one visitor), never as the constant restated to itself. → [waitlist](docs/waitlist.md)
- **One signing secret, and it's a TYPE (DAR-99)** — the flow's four signed values (`v1` token · `f1` claim · `r1` resume · `n1` funnel handle) are each **minted in one module and verified in another**; domain and prefix are module-private constants both ends read, so they can't drift, but the SECRET was resolved independently at **seven** call sites and nothing proved they agreed — the unit specs round-trip _inside_ one module with the secret passed in, and the hermetic e2e has no reachable DB, so the enrich and the funnel insert are no-ops there while token and claim are checked only by **shape** in the hidden fields (DAR-81's two-gates-failing-closed-into-a-pass, again). A mismatch is silent four ways, none an error anyone sees: funnel records views only ("nobody converts"), steps stop enriching, everyone routes to branch B, reloads drop to the blank form. Fix is one resolver **plus a brand**, and the split matters — one resolver alone only makes a **rename** safe, while the brand is what catches the other two drifts (a per-purpose secret at one end, a new entry point resolving for itself), both of which compile fine against `secret: string`. It **found a real one immediately**: `resolveStepRow`/`rememberStep` typed the secret as a bare `string`, so every step's token and cookie crossed an untyped hop. Four things worth keeping: (1) the resolver needs its **own module** — `readEnv` pulls in `$app/server` and the four signing modules are deliberately request-free, which is what lets their specs round-trip without a request; (2) the brand is erased at runtime so a **cast** defeats it, and the cast is exactly what someone reaches for here, so the spec pins that the brand is **minted in one file** just as the flow id is — naming a DIFFERENT key (`readEnv('X') as WaitlistSigningSecret`) passes a key-name scan and satisfies the compiler, and only the unavoidable cast gives it away; (3) the spec **derives its file list from the directories** — the first cut hand-listed paths and deleting one entry made the scan blind while the suite stayed green (**measured: 7/7 passing against a drifted file**), the defect every path-list test has; and it reads via `node:fs`, not `import.meta.glob`, because a raw glob asks Vite to load `.remote.ts` and SvelteKit's remote-module plugin refuses them — the four files that matter most; (4) comments are **stripped before scanning**, since these files discuss the key in prose constantly, so a raw-text scan would either trip on every explanation or be loosened until it caught nothing. → [waitlist](docs/waitlist.md)
- **A negative assertion against a `waitUntil` needs a happens-AFTER, not a sleep (DAR-103)** — `pnpm smoke:waitlist` walks the whole v2 flow against a real database, which is the composition neither suite can reach: the unit specs round-trip mint→verify INSIDE one module with the secret handed in, and the hermetic e2e's placeholder `DATABASE_URL` makes the enrich and the funnel insert no-ops **by design** while it reaches the token-gated steps through the honeypot **decoy** token, which addresses no row — so the token and claim are checked only by SHAPE in the hidden fields. Six things worth keeping: (1) the script shipped **two vacuous assertions before it shipped anything else**, and both were found by mutation, not by reading — every write it asserts on is fire-and-forget in `ctx.waitUntil`, so "assert X did NOT happen" read straight after a POST is a race the script wins by accident (DAR-81's pattern, in the one place a `waitUntil` makes it easy to write without noticing); the row disproving the first arrived **235 ms** after the read, and the forged-flow probe stayed **green** under the exact DAR-86 regression while the run failed later on a different assertion with a misleading message. The fix is never a longer sleep (that encodes a duration measured on one machine) but an **anchor**: something the run does later, through the same request path into the same database, whose arrival means the earlier write had its chance — and where no later event exists (the Priority-A claim) a **re-read at the end of the run** is the wait; (2) it was also **wrong on the merits**, which the mutation surfaced and the reading hadn't: it reloaded `/waitlist` BEFORE step 1, where there is no resume cookie, so two arrivals really are two flows and two views — DAR-75's "a reload keeps the flow" only holds **mid-flow**; (3) a specific check must run **before** a general one that also catches its failure, or the forged flow's rows surface as "reload: expected 1, got 2"; (4) it is a **client**, so it parses no signed value — the funnel is found by reading the **database's own clock** first (not `Date.now()`; the rows are stamped by SQLite on a Turso host) and asserting every row since belongs to ONE flow, which is stronger than "our rows are there" because it also catches DAR-75's `__data.json` over-count; (5) it **seeds the lead** so `isNew` is false and the signup mail never fires — not a dodge, that IS DAR-88's append-only case — while the one mail it does send (Priority A) is disclosed rather than suppressed, being inseparable from the claim it asserts (the Resend key is checked BEFORE the claim, so a silent run is a run where the column was never stamped); (6) two mutations paid for themselves: undoing DAR-86's verification breaks the **resume cookie before the funnel** (the signing core splits on `.` and the cookie carries the bare id), and casting past the `WaitlistFlowId` brand at one call site takes the step funnel **silent rather than dirty**, because `isWaitlistFlowId` inside the capture is the runtime half that makes it fail closed. → [waitlist](docs/waitlist.md) · [commands](docs/commands.md)
- **Waitlist v2 step-write budget (DAR-68)** — steps 2–4 are unauthenticated writes the continuation token reaches for ANY submitter of a known address, and step 1's throttle can't see them (it counts **rows created** per hashed IP; a step adds no row), so a holder could drive unbounded `UPDATE`s at one row. The cap is **per ROW** (a token addresses exactly one row — that's the abuse unit, and it doesn't punish a shared NAT), 20 writes/hour, and it is a **`WHERE` predicate on the `UPDATE` `applyWaitlistStep` was already making** with the counter in two columns on the row itself (`step_write_count`/`step_write_window_at`; SQLite evaluates `SET` against the **pre-update** row, which is what lets the counter read and replace itself in one statement). Three properties are the reason for that shape: (1) **no extra query** — a permitted step costs what it did before and a refused one costs one zero-row `UPDATE`, strictly LESS than the write it replaces, whereas a counter table (better-auth's `rate_limit` was the candidate) spends a read + a write to refuse a write, i.e. protects the DB by hammering it, and gets more expensive exactly as abuse worsens; (2) **no oracle, structurally** — the guard isn't a check that runs before the write and returns a verdict, it IS the write, so no code path exists that could leak token validity: refused / decoy / expired / deleted-row / success are one generic response (`updated: false` is deliberately the same answer as "no such row"); (3) the cap is far **above** a real ceiling (the flow is three writes) rather than snug against it, because the refusal is SILENT — a tight cap would eat a visitor's answers with no error — pinned by a spec that walks the whole flow five times. Fixed, not sliding: a write inside a live window doesn't move its start and a **refused write touches nothing**, so hammering can't extend its own lockout. Two things it does NOT bound, deliberately: request **volume** (edge/WAF territory — rotating tokens sidesteps anything at this layer) and — until **DAR-86** signed it — the **funnel insert on the same endpoints**, whose `flow_id` was client-minted so rotating it defeated the composite-key cap. → [waitlist](docs/waitlist.md)
- **Waitlist submissions are APPEND-ONLY, collated to a lead under human review (DAR-88)** — a repeat email inserts a NEW `waitlist_submission` under the same `waitlist_lead` instead of collapsing into the existing row, and that one decision dissolved most of the write-policy machinery above it: **all of DAR-72** (`phone` fill-forward, `contact_permission` decline-wins), the `fillIfEmpty`/`grantFillsDeclineWins` pair and the per-column "is this actionable or judgement" taxonomy, `upsertWaitlist`'s enrich branch, and DAR-87's throttle-exempt enrich (there is no enrich; every submit is a row the per-IP count now sees). The premise those existed to contain was that **two actors could write one row** — step 1's anti-enumeration response returns the same success shape, token included, for a new and an existing email, so a repeat email handed the second submitter a token bound to the FIRST submitter's row. Now every submit really is new, so anti-enumeration is TRUE rather than a cover story, a token addresses the row its own holder created, and one rule remains (`coalesce(new, existing)`, provided-wins) as a UX nicety — don't lose a visitor's own answers when they walk back a step — not an authorization policy. Six things worth keeping: (1) **`isNew` gets MORE load-bearing, not less** — it gates the ack/lead emails, and "a row was created" is no longer evidence of a new person, so it must ride the LEAD's `insert … onConflictDoNothing().returning()` against the `lower(email)` unique index (which MOVED there from the signup row) — the DB decides atomically, so two concurrent first-signups still can't both mail; a "count the submissions for this email" implementation would race and mailbomb. (2) **The lead holds NO answers** — identity + invite state (DAR-67's three columns moved onto it) + `reviewed_at`/`reviewed_by`; a merged-answers column set was rejected because it is the same overwrite problem with a friendlier UI, so reconciliation lands in what the operator does next, not in a column. (3) The lead's band is the **strongest any single submission earned** (`classifyWaitlistLeadGroup`) — merging fields first could assemble a Priority A from a role one person gave, a timeline another gave and a pilot answer a third gave, i.e. a lead nobody is. (4) Conflicts are **flagged, never resolved** (`waitlist-collate.ts`): every answer column is compared (a type guard forces `WAITLIST_CONFLICT_FIELDS` to stay complete — an unlisted new column would silently never be compared), a `null` never conflicts (absence isn't disagreement, or progressive disclosure would flag everything), multi-selects compare as sets. (5) The invite's **name comes from the EARLIEST submission that gave one** — newest-name would let a stranger choose the greeting on mail we send to the real person's inbox. (6) Costs accepted: repeat submitters grow the table and a stranger can bury a real signup under junk rows — bounded by the per-IP row-count throttle plus a per-submission delete (`?/deleteSubmission`, separate from the lead delete's cascade), with no retention cap; poisoning didn't vanish, it MOVED to a visible row a human can dismiss. The hand-ordered migration `0010` seeds a lead per existing row and re-inserts it as that lead's first submission **keeping its id** (live continuation tokens embed it). → [waitlist](docs/waitlist.md)
- **Re-scoping a stored question needs DISJOINT slugs and labels that carry the retired scope (DAR-126)** — step 3 asked for an **annual** budget, a number nobody can answer credibly before a scoping conversation, over annual-shaped bands (`under-5k`…`over-500k`) that put a realistic evaluation at the bottom of the ladder where the facet stopped discriminating; it now asks what could go behind an **initial evaluation or pilot**. Append-only (DAR-88) makes the data half easy — old rows keep their slugs, no migration — and that is exactly what makes the rest subtle: `budget_range` now holds answers to **two different questions**. Four rules: (1) the new figure slugs are **disjoint** from the retired `WAITLIST_ANNUAL_BUDGETS`, because `waitlistBudgetLabel` is keyed on the UNION of both sets and a reused slug would collapse to one entry with one label, silently mislabelling every row answered under the other scope — a union key is only unambiguous while the sets are disjoint, which no type can see, so it is a spec; (2) the retired bands **keep resolving** (`role`'s rule — a legacy slug rendering as a raw slug is the silent data loss) and their labels **say "(annual)"**, since the operator reads the value and $25k–$100k a year is the opposite buying signal from $25k–$50k for a pilot; unmarked therefore means the field's stated scope, which holds only while the field SAYS it — so the two surfaces that render a band with no question beside it (the form's label, the admin column) are pinned to the help text's word, a hole every other assertion here passed straight over; (3) `not-involved-in-purchasing`/`not-sure` carry over deliberately — they describe the respondent, not a figure, so one label is right under either scope, which is also why the retired list holds figures ONLY and the disjointness assertion needs no allowlist; (4) the step-3 validator refuses the retired bands, so an old slug can't arrive as a new answer, and the e2e pins the SSR'd `<select>`'s option VALUES against `WAITLIST_BUDGETS` — the only test of the structural claim that a wider label map can't leak a retired band back into the form; (5) the retired list is **append only**, and that direction needs its own guard because deleting an entry makes the rule BLINDER while disjointness still passes (DAR-102's polarity) — a restated copy would be a tautology, so each retired slug is held against the `waitlist_budget_annual_*` message that exists for it and nothing else, a cross-artifact pin that reports a one-line deletion in either direction. The copy assertion pins the question against **both** money questions — impact is still genuinely annual, so "no waitlist question says annual" would pass and be wrong. Nothing in the rubric moved: DAR-65's classifier structurally cannot see `budget_range`. → [waitlist](docs/waitlist.md)
- **Waitlist v2 reload-resume (DAR-75)** — the flow was rendered entirely from remote-form **results**, which are per-response, so a reload of `/waitlist` showed the blank step-1 form again; the state now rides a **signed, httpOnly `waitlist_resume` cookie** (`$lib/server/waitlist-resume.ts`) carrying `stage|submissionId|branch|audience|cta|flowId` under a **third** signing domain + `r1` prefix on the shared `mintSignedValue` core, and the load **re-mints** the continuation token and flow claim from it rather than storing either (a cookie holding a live capability verbatim is one copy-paste from elsewhere), reading **no** DB row — so the anti-oracle property survives. `?c=<token>` in the URL was rejected outright (a row-authorizing capability in history/`Referer`/shared links is worse than the bug) and so was "a known address lands on the confirmation" (unbuildable without branching on `isNew`). Six things worth keeping: (1) verification **fails closed** on any component outside its vocabulary — the blank form is the safe answer, being what this replaced; (2) the row id is **dropped at `done`**, so a finished flow's cookie can't be turned back into a write token; (3) the restart escape hatch is **load-bearing, not polish** — resuming a finished flow would otherwise trap someone who came back to sign a colleague up on a confirmation with no form — and it is a **POST** (`restartWaitlist`) that **303s** to the bare path, rendering only on a resumed render, outside the card. It was `<a href="?restart">` first and that was wrong three ways, each a Kit-specific trap: `<body>`'s `preload-data="hover"` ran the load and **dropped the cookie on mouse-over** (DAR-66's prefetch trap, destructive this time); the redirect landed on the **same url the page's data came from**, so the client router re-rendered from cache and kept showing the resumed step though the cookie was gone; and a second `<a>` made DAR-64's one-CTA confirmation a matter of interpretation. `data-sveltekit-reload` patched the first two — the method choice removes them. **A state mutation belongs behind a POST; if one must sit behind an internal link it needs `data-sveltekit-reload`, asserted by behaviour, never by attribute** (reading the attribute passes against the broken version). The 303 also keeps the funnel honest: a no-JS remote-form POST answers with a page _re-render_, and views are GET-only, so without it a restarted no-JS visitor's new flow would record a signup with no view; (4) the **honeypot sets the cookie too**, around the decoy id, or the trap would be detectable from a response _header_ (much louder than the accepted timing tell) — and that's what keeps the reload e2e DB-free; (5) a resumed visitor keeps their **flow id**, so a reload stops splitting one visitor across two funnel flows (DAR-66) and the composite key makes the re-recorded view a no-op — which incidentally fixed a `waitlist_viewed` **over-count** nobody had spotted: Kit auto-invalidates loads after a remote-`form` submit and does it with a real **`GET /waitlist/__data.json`**, landing on the very `request.method === 'GET'` branch that records the view, so every hydrated step submit minted a fresh flow id and wrote another view (~5 rows per visitor, understating the conversion rate several-fold; the no-JS path was always right, its steps being native POSTs); (6) `path: '/'`, not `/waitlist` — locale lives in the URL, so a path-scoped cookie would vanish on a mid-flow language switch. It is **strictly necessary** (no banner) but **must be disclosed**: `privacy_collect_technical_body` enumerates every cookie the site sets, so it and `PRIVACY_UPDATED` change together. → [waitlist](docs/waitlist.md)
- **The policy now says what we DON'T send, and that claim needed a tripwire (DAR-121)** — `privacy_use_body` ended "there is no marketing list unless you joined the waitlist, and waitlist email is **only about early access**" while step 1's tick box said "Send me occasional DarcStar product and research updates", and the policy's own collection section recorded that preference — so the page contradicted the form **and itself**, one section against another. "How we use it" is now two named categories (the `items` snippet the collect/processors sections already use): **operational** mail sent because the person asked (confirmation · invitation · account), and **optional product and research updates**, which the page says we are **not sending yet** and won't until the address is confirmed by email and every message carries an unsubscribe honored **without a login**. Four things worth keeping: (1) the tick box **stays as it is** — `consent_updates` is an unverified single-opt-in claim from an unauthenticated form, so it was always a preference rather than permission; collecting one nobody acts on yet is defensible, describing a list that cannot lawfully be mailed is not, and only the second is a defect; (2) a policy is the one artifact whose truth is a **promise about absent code**, so the guard has to watch for the code ARRIVING — `email-senders.spec.ts` derives every module importing `postEmail` (the single route to the provider, itself pinned by "`api.resend.com` appears in exactly one file") and holds it against an allowlist where each declares `kind` + a per-call-site `sends` count, DAR-102's shape including its per-call-site lesson (a send appended inside an already-listed mailer would otherwise inherit that file's pass — mutation-verified, as were the aliased import, the namespace dodge and the second `fetch`). **And DAR-102's scope lesson repeated itself one directory SIDEWAYS**: the first cut scanned `src` and passed **7/7** against a marketing blast planted at `scripts/blast.ts` — the deployment boundary is the wrong boundary, because the page promises the recipient that no update goes out, not that none goes out from the worker, and a script is how a first send would actually get written here (two already mail for real). `source-scan.ts` grew a third set (`scriptSourcePaths()`, `.ts` **and** `.mjs` — a `.mjs` can still hand-roll the POST) kept SEPARATE from `appSourcePaths()`, since that one is filtered by BASENAME into DAR-99's "named in exactly one file" rule and widening it would have pulled `smoke-waitlist.ts` in; the floor assertion names a script and shape-asserts the `.mjs` half, because every other assertion here is "nothing else matched" and so a narrowed surface is invisible to all of them (mutation-verified). **And DAR-102's four walk-past routes are FIVE** — a `await import('…')` is parsed by none of them, and it is not an exotic dodge but the repo's own idiom (`scripts/gen-og.mjs` lazy-loads playwright that way), so a blast written `const { postEmail } = await import(…)` measured 7/7 green from `scripts/` AND from a route inside `src/`; `importsDynamically` closes it beside `importsNamespace`, both absolute (`toEqual([])`) because no legitimate caller needs either; (3) **`marketing` is deliberately sayable** in that union — a type admitting only the honest values makes the rule unstatable and the spec would assert that a set of strings equals itself; what the tripwire removes is the SILENT path, since a new mailer fails closed until declared and declaring it puts the word and the failure message (rewrite `privacy_use_updates_body`, bump `PRIVACY_UPDATED`, build the gate) in front of a reviewer at the moment of the decision — it cannot read intent, and says so; (4) the three gates the paragraph promises (confirmation · login-free unsubscribe · revocation, since a `false` on a later submission is explicitly **not** one) are a **ticket, not a TODO comment** (DAR-139) — the page commits us to them in public. → [legal](docs/legal.md) · [waitlist](docs/waitlist.md)
- **Better Auth gates the `/admin` submissions view (#69)** — email/password sign-in exists; **public sign-up is CLOSED again** (DAR-67 — invite-only, see below), so accounts come only from the roster UI or the waitlist invite, and the only way to make the FIRST **operator/admin** is `pnpm admin:create`. Sign-in is a **server form action** (`login/+page.server.ts`, shared `LoginForm`) that works **without JS** and forwards to `getAuth().handler()` so it stays on the router's **DB-backed rate limiter** (`rateLimit.storage: 'database'` → `rate_limit` table; a direct `auth.api` call would skip it) and forwards the session `Set-Cookie` back. `handleBetterAuth` populates `locals.user` for `/api/auth/*` + `/admin` + `/login` (de-localized match) **or any request carrying a session cookie** (`getSessionCookie` — header-only, no DB), but mounts the API only under `/api/auth/*`; anonymous visitors (no cookie) still skip the lookup (#48 win preserved). Run `pnpm db:push` (adds `rate_limit`) + `pnpm admin:create` before the admin area works. → [auth](docs/auth.md)
- **Invite-only onboarding (DAR-67)** — public self-signup is closed and accounts are handed out: `emailAndPassword.disableSignUp` is **the** boundary (better-auth rejects `/sign-up/email` at the router; hiding the page is cosmetic), while the admin plugin's `create-user` never consults the flag — which is what keeps the roster AND the new `/admin/waitlist` **invite** action working. Six rules: (1) an invitation IS a **password-reset token**, minted by hand (`activation.ts` writes the `verification` row under better-auth's own `reset-password:<token>` identifier) so it redeems through the existing `/reset-password` flow rather than a second credential path — but with a **week-long** expiry, not the reset flow's hour, which is possible ONLY because `resetPasswordTokenExpiresIn` governs what better-auth _stamps_ while expiry is _enforced_ from the row's own `expiresAt` (a spec ages a minted row and proves the rejection, or a better-auth change could silently cut every invitation to an hour while the email promised a week) — a coupling to an internal convention, pinned by a **round-trip spec** that mints here and redeems through `auth.api.resetPassword`; (2) NOT `auth.api.requestPasswordReset`, because it's capped 3/hr/IP (a staff batch isn't that threat model) and it **swallows send failures** (`runInBackgroundOrAwait` logs and returns `{status:true}`) — DAR-67 wants the opposite, so the invite is **the one awaited outbound mail in the repo** and its failure surfaces to the operator; (3) the account is created with **no password at all** (better-auth makes it optional; `/reset-password` creates the credential record on redemption), so no server-generated credential nobody chose ever exists; (4) **send BEFORE stamping** `invited_at` — the column means "Resend accepted it", never "we tried", so a failed send correctly leaves the row un-invited and retry-able (`invited_at` is the LAST send; the durable who-invited-whom history is the `[invite] activation.sent` Workers Logs line); (5) `activated_at` is stamped by `onPasswordReset`, which fires for **every** reset, so the discrimination is in the WHERE clause (`invited_at IS NOT NULL AND activated_at IS NULL`) — otherwise an ordinary reset by a never-invited lead would claim an onboarding that never happened; (6) `createUser` is called **without forwarding headers** (a trusted server-side call authorized by our `isStaff` gate) because with a request attached it demands an _admin_ session and would 403 the operators `/admin` admits — safe only because the role is pinned to `user`. Refuses to invite an address already holding a **staff** account (the button must not become a way to mail credential-reset links at colleagues). `/signup` survives as a **notice** (bookmarks) with zero actions; the "need an account?" links now go straight to `/waitlist` — **all of them need `data-sveltekit-preload-data="tap"`** (DAR-66). → [auth](docs/auth.md)
- **The invite → activation path is covered by a HAND-RUN smoke, and that's structural (DAR-80)** — `pnpm smoke:invite` (`scripts/smoke-invite.ts`) exists because the hermetic suites cannot reach it: e2e has no session cookie and no reachable DB, so the unit specs cover every piece and nothing covered them in composition (`$context` resolving in workerd, `createUser` with no forwarded headers, `onPasswordReset` firing on a real redemption, the awaited Resend send). Four things worth keeping: it mails **`delivered@resend.dev`** — Resend's test recipient, so the send is real and visible in the Resend logs but lands in nobody's inbox (`+label` gives a second run its own identity); it seeds the waitlist row **directly** rather than POSTing the public form, which would mail `info@` and hit step 1's per-IP throttle by the third run — a flake in a script whose only value is that red means something; it **purges its own leftovers before it starts**, because a run that dies halfway would otherwise leave an account behind and the next run would silently test the found-account branch instead; and it's **`.ts` run under `tsx`** (like `admin:create`) because `tsconfig`'s include covers `scripts/**/*.ts` — so `pnpm check` type-checks it against the real drizzle schema on every PR, which is the only automated signal a hand-run script gets (`.mjs` scripts get none). **DAR-91 closed the last gap and gave it a second subject**: with DAR-81 having mounted `/api/auth/*` in a preview, the script now FOLLOWS the emailed link (taking the reset path, token and `invite` flag off the callback's `location` rather than writing them down) and probes anti-enumeration **on the wire**. Four traps, each measured against a running preview rather than reasoned about: the GET callback answers the **same 302** for a live and a dead token, differing only by `?error=INVALID_TOKEN`, so the query is the assertion and the status proves nothing; the landing page must be matched on the form's **lede**, because "Set your password" is also the page `<title>` and therefore renders on the dead-link panel too — the obvious assertion is vacuous; two 429s are **byte-identical**, so an anti-enumeration check that compares bodies without first requiring a 200 reports "identical, no leak" about an endpoint that answered nothing (DAR-81's failure mode with the polarity flipped — hence status first, bodies second); and the probe belongs HERE rather than in `smoke:signin` because `/request-password-reset` **mails the address it is asked about**, so the known-good half of the pair must be able to RECEIVE — and that script's two candidates both fail it, the operator's own inbox and a throwaway at `@example.com`, whose **null MX** (`0 .`) turns every run into a hard bounce on the domain the site's real mail leaves from. Two consequences: a run now sends `SMOKE_INVITE_EMAIL` **two** emails (invitation + a real reset), disclosed rather than skipped-under-override because a smoke that asserts less depending on someone's env is DAR-79's defect again; and the probes must be anonymous, since a session cookie sends `/send-verification-email` down an entirely different branch. It spends two of a 3/hour bucket — cleared by **restarting the preview**, whose limiter is in memory, not by waiting. → [auth](docs/auth.md)
- **The 429 was untestable because every caller shared one bucket, and the bucket is a HEADER (DAR-92)** — the limiter is a real control on the public auth surface and every assertion about it was config-level: `auth.spec.ts` reads the options object and agrees with itself, while nothing showed the deployed worker refusing anything. What blocked it was that a 429 test spends an allowance, and `/sign-up/email`'s is 3/hour shared with DAR-67's boundary test — so the ticket's own options were to ship a rule that exists for the tests or to loosen a production limit. Neither was needed: better-auth keys each bucket **`<ip>|<path>`** and resolves the ip from **`x-forwarded-for`** (the header `/login` already sets from `getClientAddress()`), and nothing rewrites it between Playwright and a local wrangler — so the header the test chooses IS the bucket, which is the ticket's option 3, listed last and guessed unreachable. Five things worth keeping: (1) it **de-flaked the test that was already there** — the DAR-67 boundary probe used to spend one of those three, so a CI **retry** for an unrelated reason landed a step closer to a 429 and the 4th `E2E_REUSE_SERVER=1` run failed outright, which is the ticket's own "fails for reasons unrelated to what it asserts" defect sitting in the file the whole time; (2) **the count cannot identify the rule** — better-auth's built-in `/sign-up*` rule is `{window: 10, max: 3}`, the SAME max as ours, so "the 4th request 429s" passes just as happily against a config that lost `customRules` entirely; only the window separates them and only `X-Retry-After` reports it (mutation-proven: narrowing our window to 10 is the one edit that turns the test red); (3) **isolation is asserted, not assumed** — after exhausting one probe's bucket the test sends ONE request from a second address and requires a 400, because running the 429 test alone against a fresh preview passes with the header doing nothing at all, and both mutations (a fixed address; the header deleted) fail on exactly that line and nowhere else; (4) the addresses are **`2001:db8::/32` randomised in the third and fourth groups**, since better-auth normalises IPv6 to its /64 before keying — a /24 of TEST-NET would collide within an hour of a reuse loop and hand a "fresh" probe a spent bucket; (5) the expected numbers are **imported from `auth-options.ts`**, so raising the cap updates the test and deleting the rule is a `pnpm check` error rather than a test that keeps passing on the built-in default. Three measured facts: the default rule is **100/10s** (so the ticket's "exhaust an unlisted path" opener would have meant 101 requests, asserting a better-auth default rather than a rule we ship); the **origin check runs AFTER the limiter**, so a request with no `Origin` is refused 403 having already spent its allowance; and a **multi-token `x-forwarded-for` resolves to no ip at all** (no `trustedProxies` → single-value headers only), collapsing every such caller into one shared `no-trusted-ip` counter — stricter, not an evasion, but it is why the probe sends exactly one address. **Storage is still config-level**: a preview limits in memory, so nothing here proves the shipped `storage: 'database'` counts correctly. And reading how the key is built surfaced a production weakness the ticket never asked about (**DAR-124**, filed, deliberately not fixed here): Cloudflare re-adds `X-Forwarded-For` only AFTER all rule phases and **a Worker runs before that**, so on the direct `/api/auth/*` surface the header — and therefore the bucket — is whatever the CALLER sent; the form actions escape it only because they build a fresh sub-request from `getClientAddress()`, which reads the unforgeable `cf-connecting-ip`. Two honest caveats were recorded with it and **DAR-124 closed both**: the inference step to "a caller's value survives ingress" was measured (against prod — the limiter is its own instrument, and it all-400s), and the form actions' forwarding, which nothing guarded, now goes through one builder with a mutation-proven spec. → [auth](docs/auth.md)
- **The rate limiter's bucket key was the CALLER's to choose, and the fix's own premise needed measuring too (DAR-124)** — better-auth resolves the client address from `advanced.ipAddress.ipAddressHeaders`, default **`x-forwarded-for`**, and on a Worker that header is whatever the caller sent (Cloudflare's proxy re-adds it only AFTER all rule phases; Workers run before that), so `<ip>|<path>` was caller-chosen on the direct `/api/auth/*` surface — rotate it and the sign-in, `/request-password-reset` and `/send-verification-email` caps never bind, and `login_audit.ip_address` records whatever a caller claims, which is a forged record in the evidence an operator acts on. Seven things worth keeping: (1) **the limiter is its own instrument**, so the premise needed no echo endpoint — four `POST /sign-up/email` at PROD with a rotating header answered **all 400** while four on one fixed value tripped at the **4th with `x-retry-after: 3600`**, and the **control is what makes that unambiguous** (without it "all four 400" reads identically to "the limiter isn't running", and the `3600` also proves OUR rule enforced rather than better-auth's built-in 10s one); (2) the fix's premise — is `cf-connecting-ip` unforgeable? — the limiter **cannot** answer, since it doesn't read that header yet, so a throwaway echo Worker (deployed, hit, deleted) measured it and the answer is **stronger than "Cloudflare overwrites it": a request CARRYING the header is refused at the edge, 403 error 1000, and never reaches the Worker** — while `x-forwarded-for` and `true-client-ip` both arrive **verbatim** from the caller and `x-real-ip` is overwritten, which is why `true-client-ip` (Enterprise-only, so Cloudflare never sets it here) would have been **no fix at all**; (3) **no fallback list** — `getIp` walks the headers IN ORDER and takes the first that resolves, so a second entry re-opens the forgeable path in exactly the case that matters, and one header means an unresolvable address yields NO key rather than a chosen one; (4) **the three form actions had to change with it**, and that was the real risk — they set only `x-forwarded-for`, so a `cf-connecting-ip`-only limiter would have read nothing from them and dropped **the one path keyed correctly BEFORE the ticket** into the shared `no-trusted-ip` bucket, turning a per-IP cap into a global one with **no error of any kind**; hence the header is named ONCE (`CLIENT_IP_HEADER`, beside the `ipAddressHeaders` reading it) and the sub-request built ONCE (`auth-subrequest.ts`, which also hands `getClientAddress()` back out because `/login`'s 429 branch writes its own audit row and must use the same address the limiter keyed on); (5) **nothing tested that a form action forwards the address at all** — now three layers, each mutation-proven: the builder reads the CONSTANT (never a restated literal), better-auth's real `getIp` given the shipped options ignores `x-forwarded-for` — asserted as **"never the caller's value" rather than "null", because `getIp` falls back to `127.0.0.1` under `isTest()` and asserting null would assert the test environment** — plus two source rules that are an **INVERSION** — three cuts tried to IDENTIFY the router's callers by text and each was walked past (`auth.handler(` missed `.bind(auth)`; `auth.handler` missed **`getAuth().handler(...)`**, the most natural spelling since it needs no variable named `auth`; both passed **12/12 green** while hand-rolling the header), and deriving from `getAuth` importers is worse since eleven files import it and most reach `auth.api.*`. The fix never needed the caller list: **only one file may NAME a client-address header** (allowlist, single entry, `toEqual` so it fails in BOTH directions — DAR-102's polarity) and **no file may hand-build a request at an `/api/auth` path** (the rule that survives a caller forwarding NOTHING, where there is no literal to find). Neither cares what the instance is called, with a second assertion for what an import check cannot see (a file that imports it AND names a header on some other request — that one fired ALONE on its mutation, which is what proves it isn't redundant); (6) the e2e mirror of DAR-92's isolation test — one fixed client address, a **rotating `x-forwarded-for`**, cap must still bind — goes red on exactly that line when the config is reverted, and a FOURTH guard covers the half none of the others reach: every other test drives the direct API where the header arrives INBOUND, while the form actions **construct** it and layer 1 proves that under vitest (Node/undici `Headers`) rather than **workerd**, the runtime whose own edge refuses an inbound `cf-connecting-ip` — so a `/forgot-password` test walks getClientAddress → authSubrequest → workerd Headers → auth.handler → getIp → the bucket key and requires a second address NOT to inherit the exhausted one. Two traps in writing it: **`accept: text/html` is load-bearing** (without it SvelteKit answers a form action with its `ActionResult` envelope — HTTP **200** carrying `{"status":429}` in the BODY — so `response.status()` reads 200 for a refusal, a success and a validation failure alike; `smoke-http.mjs`/`smoke-waitlist.ts` already set it and say why), and **the probe address must not be a real account** — `/request-password-reset` MAILS a link when it is and is anti-enumerating so the test can't tell, which CI's placeholder DB hides but a local run against the real dev DB + Resend key would not; (7) an empty-string address now normalises to `null`, caught by the spec, not review: it used to reach `login_audit.ip_address` as `''`, a row reading like an address was captured when none was. Honest residual: the e2e runs against a **local wrangler with no Cloudflare ingress**, so the deployed behaviour rests on the measured edge facts plus the local test, confirmed by re-running the prod probe after deploy. → [auth](docs/auth.md)
- **The navbar reflects auth state** — root `+layout.server.ts` exposes a minimal `page.data.user` (`{ email }` or null) **plus a separate `page.data.isStaff`** (not nested in `user`, so the admin/account layouts that override `user` can't shadow it; typed in `app.d.ts`), so `Header.svelte` swaps "Sign in" for **Sign out + a dashboard link** — **Admin** for staff, **Account** for an end-user (#96). Global sign-out is `routes/logout/+server.ts` (native form POST → `auth.api.signOut` → `/`). `pnpm smoke:signin` asserts both nav states. → [auth](docs/auth.md)
- **Operator-roster management is the Better Auth `admin` plugin** — `/admin/users` (+`[id]`) does list/create/edit/role/reset-password/force-logout/disable/delete via no-JS `auth.api.*` form actions. **Three roles** (#95): `admin` (super — roster + all messages), `operator` (reads + **deletes** submissions, no roster), `user` (end-user, own account only — dormant until the #96 portal); `/admin` is gated by `isStaff` (admin **or** operator), not mere auth, and the submissions view has a staff-only `delete` action. Role helpers live in `admin-access.ts` (`ROLES`/`coerceRole`/`isStaff`/`apiRole`). **Owner bootstrap = `ADMIN_USER_IDS`** env allowlist (admin before any role check → can't be locked out; `admin-access.ts` `isRosterAdmin`); it's a runtime var, so add to `.env` + `pnpm gen`, `wrangler secret put` in prod. The plugin is **schema-affecting** (adds `user.role/banned/…` + `session.impersonated_by`) → bare `admin()` mirrored in `auth-cli.ts`, `pnpm db:push` adds the columns. Authz is **authoritative** (bypasses cookie-cache); UI self/owner guardrails aren't a hard boundary (raw `/api/auth/admin/*` has no owner concept). → [auth](docs/auth.md)
- **Signed-in page views skip the DB** — `session.cookieCache` (`auth-options.ts`, 5-min `maxAge`) serves the site-wide `getSession` from a signed `session_data` cookie snapshot, not a per-view DB read; sign-out clears the snapshot. Behavioral, NOT mirrored to the CLI config. **But** the hook resolves `/admin`+`/login`+`/account`+`/api/auth/*` **authoritatively** (`disableCookieCache`), so roster force-logout/disable are immediate there — the cache only fronts the navbar (staleness is cosmetic). → [auth](docs/auth.md)
- **End-user portal (`/account`, #96) activates the dormant `user` role** — a self-service surface (own profile via `auth.api.updateUser`/`changePassword`, own messages) **separate from `/admin`**, gated only by sign-in (no `isStaff`). Message ownership: `contact_submission.user_id` (nullable FK, `set null`), linked via `linkSubmissionsToUser` (`contact-ownership.ts`) on signed-in submit, admin-create (vouch), or self-registered email-verify (PR 2). Every `/account` query is scoped to `locals.user.id` (the load-bearing isolation). Delivered in **two PRs**: PR 1 = ownership + portal; **PR 2 = public sign-up** (`/signup`) — re-opened #48 behind **Cloudflare Turnstile** + **`requireEmailVerification`** (unverified sign-in → 403; `afterEmailVerification` runs the ownership backfill), **since re-closed by DAR-67** (Turnstile gone with the form it guarded; verification stays for pre-lockdown accounts). Staff-lockout guard: migration `0003_verify_existing_users` + roster-create/`admin:create` set `emailVerified` before it ships. **#115** adds a **resend-verification** affordance — in the login form's "verify your email" state (`LoginForm.svelte`, shared by `/login` + the navbar `LoginDialog`; a `resend` action in `login/+page.server.ts` — the signup-panel twin went with the page) → `POST /send-verification-email`, which is **already anti-enumerating** (constant-time, identical `{status:true}` for unverified/verified/absent, mails only a real unverified account), so resend works **no-JS** — don't re-implement the generic response. → [auth](docs/auth.md)
- **Every sign-in attempt is audited** — a Better Auth **`hooks.after`** middleware (`auth-audit.ts`, wired in `auth.ts`) records success + failure to the **app-owned `login_audit`** table (`db/schema.ts` — NOT a plugin table, so no `auth.schema.ts`/`auth-cli.ts` mirroring) **and** emits a structured `console.*` line → Workers Logs. Captures email/raw-IP/UA/reason/status; links `userId` on success; never logs the password; returns `undefined` so anti-enumeration is intact. Fires for the `/login` form AND direct `/api/auth/sign-in/email`; the **one blind spot is rate-limit 429s** (rejected before endpoint dispatch) — the login action records those. Persist is non-blocking (`waitUntil`, `login-audit-store.ts`, never fails sign-in). Read-only staff view at **`/admin/audit`**. → [auth](docs/auth.md)
- **Self-service password reset** — Better Auth's `forget-password` flow: `/forgot-password` (email → anti-enumerating `POST /request-password-reset`, generic "check your email") → emailed link → `/reset-password?token=…` (new-password form → `POST /reset-password`). Env-bound `sendResetPassword` + `revokeSessionsOnPasswordReset` live in `auth.ts` (email builder = `password-reset-email.ts`); **no schema change** (token in the existing `verification` table → not in `auth-cli.ts`). Both pages are **single `default` actions** (never mix default+named, #122); no captcha (works no-JS); rate-limited (`/request-password-reset` 3/hr, `/reset-password` 10/hr). "Forgot password?" link is duplicated in `login/+page.svelte` + `LoginDialog.svelte`. → [auth](docs/auth.md)
- **Security headers ship from three places** (DAR-45) — CSP = SvelteKit `csp` config in `vite.config.ts` (Kit nonces its inline bootstrap; `style-src` keeps `unsafe-inline` — transitions + SSR'd `style=` attrs can't be nonced); everything else = `handleSecurityHeaders` (`hooks.server.ts`); static assets bypass the worker → project-root **`_headers`** (NOT `static/` — the adapter errors). Shared origins/values live in `src/lib/security-headers.ts`. New third-party origin? Allowlist it in `vite.config.ts` + add a synthetic probe, or the e2e violation guard (`security-headers.e2e.ts`) fails. `pnpm preview` bakes always-pass Turnstile **test** keys (real sitekeys reject localhost). → [security-headers](docs/security-headers.md)
- **Sanity CMS content feed (DAR-14)** — `/news` (post), `/research` (paper), `/people` (person) SSR-read the `production` dataset (project `8v6ikhvv`). Typed GROQ via `defineQuery`+**TypeGen** (`pnpm sanity:types` reads `src/lib/sanity/schema.json` — **synced from `../darcstar-sanity-studio`**, NOT a local Studio — + scans `queries.ts` → committed `types.ts`; `overloadClientMethods` types `client.fetch(q)`). **Gotcha:** production is nominally _public_ but has **document-level access control** — an anonymous read sees **only `siteSettings`**; post/paper/person need a **read token**, so reads go through the **server-only** `getSanityClient()` (`$lib/server/sanity.ts`, `readEnv('SANITY_VIEWER_TOKEN')`, `useCdn:false`), never the browser. Image URLs need no token (asset binaries are public by hashed URL) → `image.ts` builds from `createImageUrlBuilder({projectId,dataset})`. `projectId`/`dataset`/`apiVersion` (`config.ts`) are **build-time** public config via `VITE_SANITY_*` (`import.meta.env`, inlined into BOTH bundles) with defaults — NOT runtime `readEnv`, because the client builds image URLs that embed the dataset, so server + client must bake the SAME value (a runtime server-only read would desync). Set `VITE_SANITY_DATASET` in the build env to switch datasets. Portable Text via `@portabletext/svelte` (`PortableBody` + custom image/code/link). New secret → `.env` + `.env.example` + `pnpm gen`, `wrangler secret put SANITY_VIEWER_TOKEN` (+ `--env preview`). → [sanity](docs/sanity.md)
- **/evidence backs the homepage stats (DAR-43)** — one dated claim card per claim (run-level depth lives on the `/evidence/benchmarks` + `/evidence/proofs` sub-pages, linked from the cards), facts transcribed from the GIDE hub's source-of-record docs; the figures + domain list are **single-sourced in `$lib/evidence.ts`** (homepage readouts, card values, and `{param}`ized message prose all read it — never re-inline a number), a value and its dated line change **together** (theorems figure = the machine-checked conformance count, 219 — never the Layer-1 "150"), **no proven-latency phrasing ever** (latency is measured, 13,000× is derived), and **never publish exact architecture numbers or the theorem backlog** — not even in source comments (this repo is public), though only **copy** is machine-guarded (`evidence-boundary.spec.ts` scans the message catalog + `$lib/evidence` constants, e2e asserts the rendered absences; comments are code-review territory). → [evidence](docs/evidence.md)
- **Claims are qualified site-wide, not just on /evidence (DAR-46)** — the published formulation is "formally verified against **stated system and environment assumptions**", never "proven/provably/guaranteed safe"; a **heading** may claim provability (both H1s are kept deliberately), the **body under it must name the assumptions and the boundary**. Second guard, sibling to the IP one: `safety-language.spec.ts` (truth boundary, both locales) — its one allowlisted key, `evidence_safety_not_covered`, quotes the banned phrase to disavow it, and a paired assertion fails if it stops. Domain counts must define the term **that surface uses** (homepage `domains running end-to-end` → `section_domains_scope`; /evidence "shipped" → `evidence_domains_claim`), both ruling out a demo or customer deployment — so nothing elsewhere may imply delivery (`about_principle_oneengine_body` says the core **runs**, not "ships"). Counts are **never restated in prose** — both surfaces render `DOMAINS.length`. → [evidence](docs/evidence.md)
- **A true number shown without its qualification is a third failure mode, and it needs a third guard (DAR-117)** — the homepage readout rendered `THEOREMS_CHECKED` in the largest type on the site while the complete/axiom-backed breakdown sat two clicks away on /evidence: nothing false, nothing overstated, nothing failing, and a formal-methods reviewer reads a bare total as inflated because most of it is checked _modulo_ local axioms awaiting discharge. The readout now leads with `THEOREMS_COMPLETE` and carries the total as a `{checked}` **param** in its label (a figure passed into prose, never re-typed into it — `pillar_realtime_body`'s pattern), /evidence keeps the full catalog, and `evidence-disclosure.spec.ts` joins `evidence-boundary` (IP: what we must not **disclose**) and `safety-language` (truth: what we must not **overstate**) as the **disclosure** axis: what we publish must not be **stripped of its qualification**. Seven things worth keeping: (1) **only the e2e can see which constant renders** — swapping it back type-checks and leaves all 981 unit tests green (mutation-measured), so the pin is `getByText(String(THEOREMS_CHECKED), {exact: true})).toHaveCount(0)` on `/`, and that assertion needs its **own** mutation to prove it isn't riding on the "31 is visible" one (a second readout reintroducing the bare total passes every other check — measured, and the first attempt at that mutation was **invalid**, since the readouts `{#each}` keys on the LABEL and two cells sharing one crashes the page with `each_key_duplicate` rather than testing anything); (2) DAR-46's rule generalizes — **each surface defines the term IT uses**, so "complete" is defined in `section_proven_body` on the page that shows it, not delegated to /evidence; (3) the ticket's own suggested phrasing, "31 complete **safety** theorems", is **false** — the complete set spans existence/stability/keep-out clusters and the safety cluster is a subset (`evidence_safety_claim` says so), which is why the label says "theorems"; (4) the honest label **wraps the row to two lines at 1440px** and that was accepted only after measuring: four across survives to about **24 characters** of label, every shorter phrasing costs a load-bearing word, and dropping "machine-checked" leaves a bare "of 219" that reads as the **catalog total** — the one figure the IP boundary exists to withhold, so the cosmetic trim would imply the thing the other guard forbids (capping the label width doesn't buy the row back either — 12rem still wraps, 11rem is worse at three rows, since the cap wraps the OTHER labels too); (5) **`pnpm preview` does not build** — it serves whatever the last `pnpm test:e2e` left behind, so the first screenshots reviewed were of a **mutation**, and any visual check must `pnpm build` first; (6) **Paraglide does NOT type-check a message's placeholders against its call sites** — the natural assumption is that deleting `{checked}` makes `m.readout_theorems_label({checked})` a type error, and **measured, `pnpm check` passes clean**, so a parameterized label's placeholder has no compiler guard at all and the spec assertion is the only thing holding it (the comment claiming otherwise was written, then disproved, in the same PR); (7) the second half is a **taxonomy, not a split of one number** — framework assumptions (Lipschitz, compact sets, bounded disturbance, hardware isolation) are hypotheses that apply at **every proof status**, the complete theorems included, since complete means "introduces no axiom of its own", never "needs no assumptions" (write it as "hold for every theorem" and it contradicts the same paragraph's "cited by the theorems that use them" — no assumption applies to all of them); local axioms are the debt and are exactly what keeps a row out of the complete count; a carried **physical** premise means the row is not counted as proven at all. `/evidence/proofs` states all three with **no counts** (how many carry what is the backlog) and **no proportions** — how the local axioms divide between "mathematics the prover libraries lack" and "a result proved elsewhere in the corpus" is not something this repo can source, and an unmeasured "mostly" is the same defect as an unmeasured figure on the page that exists to not have them. The named assumptions are pinned against `evidence_theorems_not_covered` so two pages can't list different premises. → [evidence](docs/evidence.md)
- **A guard written in terms of the number it hides fails in both directions at once (DAR-152)** — the IP boundary's catalog-total rule was `/\b338\b/`, labelled "the theorem-catalog total (338)", so the spec **wrote the secret into a public repo in order to guard it** AND pinned it to a vintage: the corpus has grown, so the literal guarded a figure that is no longer the leak while today's total sailed through (mutation-measured — a planted `346` passes the old rule, fails the new one). One cause, one fix: derive the boundary from the figure we **publish**, since every publishable theorem count is at most `THEOREMS_CHECKED` and the catalog size is by construction above it, so the band names nothing and a re-measure carries it along. Seven things worth keeping: (1) **each half of the value rule is load-bearing, and which halves were needed came from measuring the catalogs, not from reasoning** — dropping the **band** leaves `Lean 4` reporting itself in 7 keys, while dropping the **proximity** test leaves the benchmark iteration counts reporting themselves (8 hits over 5 keys, led by `evidence_cfc_method`'s "1,000 warmup iterations") — a direction that is genuinely slippery, since "the band alone flags X" is the INVERSE of "dropping the band flags X": these notes were written inverted, corrected, and re-inverted BY the correction, each caught only by re-reading against the measurement rather than against the previous sentence; both halves also need the **calendar-year** exclusion because each dated line puts a year beside "corpus"/"theorems"; the bare-integer rule (no digit glued to a word, hyphen or dot) is what drops `SHA-256`, `0.767`, `p50` and `Isabelle2025-2`, none of which counts anything; (2) a candidate pattern was **rejected on measurement**: "not yet mechanized" fires on DAR-117's own `evidence_proofs_axioms_local_body`, which says the prover libraries "do not yet formalize" a result — a fact about Mathlib, not our backlog, and the kind of false positive that gets a guard loosened until it catches nothing; (3) the **arithmetic route** is a real second leak — a count beside "75.4% of the catalogued corpus" (the hub brief's own phrasing, so a copy-paste would carry it) recovers the total to within a row while every value check stays green; (4) the **complement cannot be guarded by value at all** — the un-mechanized remainder is the total minus a published figure, so it sits BELOW the band, and computing it would mean importing the secret this module exists to stop naming; wording is the only lever and that half is **honestly partial** (plain spellings, not a paraphrase), which is stated rather than papered over; (5) the detector is **one module** (`evidence-boundary.ts`) because the unit spec scans catalogs + constants and the e2e scans rendered text — a hand-kept second copy is DAR-99's rot — and over rendered text the unit is **a line and its successor**, both bounds measured rather than reasoned: rendered text is a _concatenation_, not prose, so a whole-page scan reads the homepage's `13,000×` readout as neighbouring the theorems one — while one line at a time misses the shape the claim cards actually use, a bare value above its label, exactly how a total would be published (`346` over "Theorems in the catalog" passed all six evidence e2e tests). A pair reunites value with label and stops short of the collision, `13,000×` rendering three lines from the theorems readout; (6) **`String.slice` reads a negative start as an offset from the END**, so an unguarded `at - WINDOW` tests the tail instead of the hit's neighbourhood and misses a leak opening a long paragraph — invisible to every test written first, because each was shorter than the window so the negative start clamped to 0 (**reinstating the bug passed all 31**; the case that catches it opens a long line with the leak); (7) the e2e backstop is **non-vacuous, measured** — a total hardcoded into a `.svelte` rather than the catalog passes all 32 unit tests and fails the e2e, naming the page and quoting the sentence. Three more from later review passes: (8) **the constant that defines "near" was itself unpinned** — 20, 200 and 400 each passed all 39 tests, so the proximity half had no test at all in either direction, and both bite (too small stops catching, too large matches a benchmark figure against theorem wording elsewhere in the paragraph); two cases now bracket it to (44, 117) and `CONTEXT_WINDOW = 60` is documented as a **choice inside a tested range**, not a derived value. (9) **a test can cover a route by accident and look like it covers it on purpose** — the corpus-percentage case used the hub brief's own phrasing, whose count sits ABOVE the band, so route 1 caught it and route 2 could be deleted with every test green; each route needs a case where it is the ONLY signal. (10) **a heuristic guard needs different on-a-hit guidance from an exact-value one** — "reword, never loosen" is right for `40,824` and wrong here, since "500 trials against the theorem conformance registry" fires and is not a total: check the figure against the source of record first, and if it genuinely is not the total, narrow a context term rather than widening the band. Rules-of-thumb: **a scan whose assertions are all "nothing matched" passes just as happily against a detector that answers nothing** (so the detector needs its own positive cases), and **never name a forbidden value in a public repo — derive it from a published one**. → [evidence](docs/evidence.md)
- **/research is origin-split (DAR-52)** — papers render by `darcstarAuthored` with **fail-safe polarity**: unset/null = third-party (`PaperOrigin` chips + the `PaperExternalDisclaimer` statement, grouped under "Foundational reading"), never first-party; the `commentary` Portable Text renders through the same `PortableBody` as post bodies. → [sanity](docs/sanity.md)
- **A tooltip is not a rendering (DAR-56)** — the Studio's `topic.description` says "shown alongside the papers tagged with it" and reached nothing but a `title` attr on the `PaperTopics` tags, so all ten authored descriptions were invisible on touch, unreachable by keyboard, and unreliably announced; `TopicGuide.svelte` renders them on /research as a collapsed `<details>` legend **plus** — when `?topic=` is set — that topic's description **plainly visible, outside the disclosure**, because a disclosure there is the tooltip's problem in a new costume (it's the surface that closes the touch loop: tap a tag → filtered view → read it). Six things worth keeping: (1) the ticket's own option B was premised on a `topicsQuery` that exists only in an **unmerged Studio worktree branch**, and it wasn't needed — the papers fetch already projected `topics[]->{…, description}`, so the guide derived from data the page had. (**DAR-94 superseded the mechanism, not the point**: the descriptions now come from a topic-vocabulary projection, because a paginated fetch can only describe its own page. What survives is `topicOptions` projecting the select's shape from the SAME list, so "which topics does this index have" still can't answer differently in two places, and `partitionByOrigin`/`isDarcstarAuthored` keeping the origin split one predicate instead of three spellings.); (2) the legend reads the **whole in-use taxonomy**, never the rows in view — it explains the taxonomy, so filtering to one topic (or paging) must not shrink it to what is on screen; DAR-94 turned that from tidiness into a requirement; (3) it renders **nothing, not an empty wrapper**, when no topic has a description (the page's `space-y-8` is `> * + *` → a phantom gap); (4) the spec is only meaningful because the `client` vitest project runs **real chromium** — it separates _visible_ from _in the DOM_, which IS the bug, and a jsdom run would pass against the broken version, so it proves the closed `<details>` hides its body **both directions** before relying on that; **`getByRole` finds nothing inside a closed `<details>`** (it's absent from the a11y tree — measured, it's what failed first), so DOM queries, not role queries, for legend internals; **and never put a `glass-*` surface inside a collapsed container** — a closed `<details>` hides its body with `content-visibility: hidden`, which does NOT zero `getBoundingClientRect()`, so the sheen clipped a **768×689 window over the paper cards** for a panel nobody could see (found in review; fixed structurally by wrapping the whole disclosure in the card, so opening it merely GROWS a panel — the case the sizeObserver already handles — and pinned by a spec, → [styling](docs/styling.md)); (5) **no e2e on purpose** — CI runs Playwright without `SANITY_VIEWER_TOKEN`, so /research is empty there and the guide is correctly absent; (6) this is the **first live `summary:focus-visible`** on the site (DAR-57 called it a dead selector — every other `<summary>` is in `/admin` and opts out), and measuring it re-confirmed DAR-57's transition trap on a new element: the immediate read returned the summary's own text colour, settling to `--color-primary-400` only after `transition-colors`' **`outline-color`** finished. → [sanity](docs/sanity.md)
- **Paginating an index moves its facets, not just its rows (DAR-94)** — `/research` and `/news` fetched every published document and rendered a card each, on every SSR request (161 KB of HTML and a 44.6 KB payload for 18 papers; now **125 KB / 25.8 KB**, and flat in the corpus). The reason it wasn't "add a slice" is that /research's facets **derived from the fetched set**, so slicing the fetch would have shrunk the Topic select and DAR-56's topic guide to whatever 20 papers were in view — they now come from the taxonomy's own in-use vocabulary in the same round trip, which makes the guide **more** correct than before. Nine things worth keeping: (1) **`defineQuery` must get a CONST-INTERPOLATED template, never a function call** — `defineQuery(build(order))` type-checks AND TypeGen emits a correct `…Result`, but TS infers the argument as plain `string`, `overloadClientMethods` fails to resolve, and `client.fetch()` silently returns **`any`** (proven: `const s: string = result.total` errored for the const form, PASSED for the function form); (2) GROQ's `order()` can't be parameterised, so the sort picks between three literals whose only textual difference is that clause — pinned by a spec that **strips it and compares byte-for-byte**, because the three `…Result` types stay structurally identical even if someone edits a FILTER predicate in one, i.e. types cannot see the drift; (3) **origin became the MAJOR sort key** (`select(darcstarAuthored == true => 0, 1) asc`) — DAR-52's section split was a render-time partition of a date-sorted list, so a page could otherwise straddle the boundary and the headings would describe a subset of what sits under them; this reproduces today's rendering exactly; (4) **`?page=` is deliberately NOT in `FILTER_PARAM`**, and that omission IS the "changing a filter resets to page 1" rule (`buildFilterQuery` emits only keys it finds there; the no-JS GET form replaces the whole query string) — a guarantee invisible in the source, so it is a test, not a comment; (5) **branch on the TOTALS, never `data.papers.length`** — that's one page now, so an empty index and a zero-match filter look identical, and gating the filter bar on the page's rows takes the bar away with the results; (6) **`defined(abstract)` as a FILTER disagrees with the same expression in a PROJECTION** — measured reproducibly across two API versions, `count(*[… && defined(abstract)])` answers 6 while all 18 papers return `defined(abstract): true` from a projection, so the 50-word cut doesn't gate on it; the alarming reading is that every `defined()` guard on the site is unreliable, so the other fields were checked rather than assumed — **`slug.current` (papers AND posts, the guard every list query depends on), `title`, `venue` and `darcstarAuthored` all agree**, confining the oddity to the one long-text field (**DAR-95 later found the documented cause**: Sanity's GROQ reference says `defined()` "will not provide the expected result" above **1024 characters** — the flagged abstracts measure 1186/1452/1664 — so the rule has a threshold now, not a shrug: never `defined()` a field that can exceed 1024 chars); (7) the cut is invisible because the card `line-clamp-3`s anyway (measured in a real browser at 390/768/1440 px — every truncated abstract still overflows), plus a `…` in the DATA for the one case measurement can't rule out, a short-worded abstract that fits; (8) query views **already** canonicalise to the bare path (`Seo.svelte` derives from `page.url.pathname`, which excludes the search string) — true before this ticket, which is exactly why it now has an e2e; (9) **title collation stepped down deliberately** — GROQ orders by code point, `lower(title)` buys back case but not the accent-insensitivity `localeCompare(…, {sensitivity:'base'})` had (zero visible change on today's corpus; **restored by DAR-95**, below). The **author facet is the one thing pagination doesn't flatten** (123 people for 18 papers against 134 author slots — ~7 new per paper, never plateauing, ~96 KB at 300), so it became a text input over a `<datalist>` seeded with team authors and refilled from `/research/authors.json` at 3+ chars; the floor is enforced **server-side**, because `match ("" + "*")` and `match ("*" + "*")` each return all 123 people — without it the endpoint hands out the vocabulary the input exists to keep off the page. → [sanity](docs/sanity.md)
- **A sort key is a publication artifact, and the reachable defect was PEOPLE (DAR-95)** — DAR-94 moved ordering into GROQ, which sorts by code point and **cannot normalize a string at query time** (`string::` has only `startsWith`/`split`, `order()` takes no collation argument, and custom GROQ functions are projection-shaped), so restoring the accent half of `localeCompare(…, {sensitivity:'base'})` needs a **stored** key: `paper.titleSortKey` + `person.nameSortKey`, derived by the Studio's `pnpm promote`, read as `coalesce(<key>, lower(<source>))`. Six things worth keeping: (1) the ticket was scoped to paper titles and **today's corpus cannot trigger that** — no paper title carries a diacritic, while `Łukasz Kaiser` sorted **last of 123 authors**, after every Z, so the half that mattered is the half the ticket didn't ask for; measuring the data before implementing is the only reason it was found. (2) **`Ł` (U+0141) has no NFD decomposition**, so the ticket's own `NFD` + strip `\p{Diacritic}` would have fixed nothing reachable — stroke/ligature letters need an explicit folding map (`ł→l · ø→o · ß→ss` …), and what results is an approximation of base-sensitivity collation for **Latin** scripts, never ICU (non-Latin still sorts by code point, as it already did). (3) It's derived at **promote** — the chokepoint every published document crosses on its way to the dataset the site reads — and **recomputed from the document's own text, never copied through**, because a stale key is WORSE than none: it sorts a renamed document under its old name, where `lower(title)` is at least always self-consistent. Hence `dev` deliberately carries **no** key; it's a publication artifact like the private-field stripping in the same pipeline. (4) **Keep the `coalesce`** — measured, it returns today's 18 titles **byte-identically** to `lower(title)`, which is what lets the two repos ship in either order and makes an un-promoted document degrade instead of jumping to the front under a null; the spec pins it by **counting** every mention of a key against the coalesce expressions containing it (DAR-104 widened the right-hand side to enumerate `match` arms too, rather than relaxing the assertion — ordering is where a missing key silently mis-places a row, matching is where it merely doesn't fire), because `order(titleSortKey asc)` type-checks, satisfies a naive "a coalesce appears somewhere" assertion, and silently breaks every un-keyed row. (5) The three by-name orderings share **one const** (`ORDER_PERSON_NAME`); it matters most on `/research/authors.json`, which is **capped at 12**, where a name that mis-sorts to the end isn't merely misplaced — it's absent. (6) The Studio has no test runner, so the normalizer carries a table-driven `selfTest()` that `check:content` runs — **not redundant with the drift check**, which calls the same function and would therefore agree with a wrong one and report clean data. → [sanity](docs/sanity.md)
- **A stored sort key also answers "did they mean this person?" (DAR-104)** — GROQ's `match` compares code points just as `order()` does, so author SEARCH stayed accent-sensitive long after DAR-95 fixed the ordering: measured against production, `luk` found nothing while `Łukasz Kaiser` sat in the index, and the same held for `re`/`Christopher Ré` and `konighofer`/`Bettina Könighofer` — **every** accented author in the corpus was unreachable by the spelling an English keyboard produces, with no signal that the query was the problem (`dao` 4 · `da` 8 · `gu` 2 · `tri-dao` 4 all unchanged, so the fix is a targeted superset, and DAR-100's retired `?author=ukasz-kaiser` stays 0 because `ukasz` isn't a token prefix of `lukasz kaiser`). `PAPER_MATCH` and `authorSuggestionsQuery` each gained a `nameSortKey match` arm beside the `name` one. Four things worth keeping: (1) only the **filter** half was verified end-to-end — the type-ahead arm makes the ENDPOINT answer `?q=luk` with `Łukasz Kaiser`, but `/research`'s control is a native `<datalist>` that applies its OWN matching to the options it is handed, against the accented display name, so an accent-sensitive browser filter would hide the row the server just found; unverifiable with the tooling reached for at the time (the popup is browser chrome — absent from screenshots, unresponsive to synthetic keys, both probes failed their controls) so it was recorded **open** rather than claimed, and it cost nothing meanwhile because the datalist is progressive enhancement and `PAPER_MATCH` carries the same arm — **DAR-105 measured it, and the browser was indeed hiding the row**; (2) the arm is **additive, never a substitution**, so a key-less document (`dev` carries none; the key is a publication artifact) just fails that arm and the predicate degrades to exactly the old behaviour instead of erroring — measured, same fail-safe polarity as the origin flag; (3) the two call sites **cannot share one expression**, and that's DAR-94's constraint rather than laziness — a shared builder function would widen the query to `string`, break `overloadClientMethods` and make `client.fetch()` return `any` — so the arms are written twice and the spec **counts** them, making "widened one, forgot the other" a failure instead of a silent desync between the type-ahead and the filter it feeds; (4) the suggestion arms **must stay parenthesised** (`&&` binds tighter than `||`, so without the group the published-papers filter would apply to only the second arm) — a precedence bug no type can see and the counting pin does NOT catch, since the counts stay equal, so it has its own mutation-verified assertion. → [sanity](docs/sanity.md)
- **A native `<datalist>` re-filters what the server found, and the two engines disagree about WHAT (DAR-105)** — DAR-104 made the author endpoint accent-blind and the browser undid it: measured in headed chromium and firefox, the popup's own matching is a case-insensitive **substring test over code points**, so `luk` matched nothing in `Łukasz Kaiser` and **no popup appeared at all** — the fix was correct at the endpoint and invisible in the UI, exactly the risk DAR-104 recorded open. Six things worth keeping: (1) **the instrument was the whole problem, and the third one worked** — driving the popup with the browser-automation extension failed its control (DAR-104), driving it with Playwright's real key events failed in chromium and worked in firefox, and what settled it was to stop driving the popup and **photograph** it: it is a separate X window, which is why a page screenshot never contains it and an **X root-window capture does**. Two traps on the way — Playwright's chromium runs as a native **Wayland** client on this desktop, so it has no X window to capture (`--ozone-platform=x11` + `xvfb-run`, since `import -window root` cannot read an Xwayland root either); (2) a probe of an unspecified behaviour needs **both** controls — a positive (an ASCII option, exact prefix → popup must appear) and a **negative** (a term matching nothing → popup must NOT appear), or "no popup" is indistinguishable from "the camera is pointed at the wrong thing"; (3) the engines are **opposites**, and that is the load-bearing measurement: chromium matches `value` OR `label` and displays value bold with label grey beneath, while **firefox matches ONLY the label when one is present** and displays it _instead of_ the value. So the obvious fix — put the ASCII slug in `value`, the real name in `label` — works in chromium and renders **nothing** in firefox. Measured, not reasoned, and it was the plan of record until the pictures came back; (4) therefore the label is the accent-blind match target and must carry **both** spellings (`Łukasz Kaiser (lukasz kaiser)`), or making `luk` work costs `Łuk` the suggestion in firefox — one unreachable spelling traded for another, which is not a fix; (5) **`value` is untouched**, so every URL the control can produce is byte-identical and the label is emitted **only** for a non-ASCII name (120 of 123 authors keep no attribute at all — emitting one would be a pure regression, since firefox would start rendering `Tri Dao` as its lowercased sort key); (6) the folded string is **read from the document** (`nameSortKey`, a third legitimate use of DAR-95's key, enumerated in the counting pin rather than relaxing it) rather than derived in the browser — `NFD` + strip `\p{Diacritic}` is the reflex and it fixes `Ré` and `Könighofer` while leaving the headline `Ł` exactly as broken, so a second copy of the Studio's folding map is both wrong and undetectable here. The emit condition is a **containment test, not an accent test** (`label` unless the name already contains the key), which is what makes the guarantee structural in BOTH branches: the server matches a token **prefix** of `name` or `nameSortKey`, the browser matches a **substring** of the label where there is one and of the name where there is not, and either the label carries both strings whole or the name already covers both — so the list can never offer LESS than the filter finds. That phrasing also catches a case an accent test would miss (whitespace `sortKey` collapses, `Tri  Dao` → `tri dao`) and skips one it would wrongly catch (a CJK name folds to itself). Honest residual: **WebKit is unmeasured** (playwright's build cannot launch — missing host deps), but the change is safe there by construction, since matching can only gain a second target it may ignore. → [sanity](docs/sanity.md)
- **`siteSettings` is one-of-ten wired, and that's the design (DAR-73)** — the Studio singleton is a five-tab editing surface the site never queried, so a `socialLinks` edit adding LinkedIn+BlueSky was published AND promoted and rendered nowhere. Only **`socialLinks`** is live (the footer row + the Organization `sameAs`, from ONE resolved list so the rendered links and the machine-readable identities can't disagree); the other nine stay inert with **`docs/sanity.md`'s table as the source of truth** — `contactEmail` is the Resend **From:** address on five mailers and `primaryNav`'s live value points at three routes that 404, so wiring them is deliberately **not** planned (trimming them from the schema is the deferred half). `siteSettingsQuery` projects **only** `socialLinks`, so a new Studio field can't silently start shipping to the client. This is the **only Sanity read on every page's request path** (the footer is in the root layout → it also runs on `/admin`, `/login`, every form POST), affordable solely because of three load-bearing properties: a 5-min **module-scope TTL cache** (safe ONLY because the payload is public and identical for every visitor — the same cache over per-user data is a cross-request leak), a 2 s **`AbortSignal.timeout`** (else a hung Sanity stalls the whole site, not one route), and a **floor** to the hardcoded `GITHUB_URL` on any failure, negative-cached 30 s so an outage stays cosmetic. The floor applies to an **empty** result only and is never merged into a non-empty one, or an editor couldn't delete the GitHub link — the same lie inverted. URLs pass `isHttpUrl` (DAR-70's gate) at both hops: the Studio's `rule.uri` is a UI affordance an API write skips. → [sanity](docs/sanity.md)
- **A renderer whose output is STATIC belongs on the server, and a type is what keeps it there (DAR-106)** — the Studio shipped `mathInline`/`mathBlock` weeks ago and the site rendered neither, silently, because `PortableBody` passed `onMissingComponent={false}` and the library's unknown-**object** component renders an object's non-existent children (which is also why the Studio's own always-on "not rendered yet" warning existed, and why removing it is a paired Studio PR **after** this deploys, never with it). The ticket sketched `katex.renderToString` inside the component; measured, that is **76.3 KB gz** of JavaScript on `/news/[slug]` + `/research/[slug]` against **+661 B gz per equation** for the copy of the typeset HTML that Kit serializes into the hydration payload beside the SSR markup — **both** approaches SSR the markup, so that half cancels and the real trade is one payload copy per equation against the library once, crossing over at ~**115** equations on one page (the corpus has **zero**). That figure is measured by stripping the payload's `html` values out of the real four-equation page and re-gzipping (12,813 → 10,168 B gz), not derived: the same number computed from isolated strings came out ~15% low, and a first draft divided the library by the WHOLE per-equation cost rather than by the delta — two errors that happened to nearly cancel, which is exactly why a plausible-looking headline number needs its own measurement. Six things worth keeping: (1) `$lib/server/math.ts` makes "the browser never gets KaTeX" structural, DAR-65's move, and the client-safe half is a **type alone** (`$lib/sanity/block-content.ts`) purely because a component cannot import from `$lib/server`; (2) that type is the real gate — `html` is **required** on the math members, so a route that forgets to typeset **fails `pnpm check`** rather than rendering nothing, which is the very bug being fixed, and its spec pins it with a `@ts-expect-error` that turns _unused_ the day the conditional type stops discriminating; (3) **never open a `//` comment line with the directive's own name** — a second mention at the start of a line is a second REAL directive and it swallows the error the first was placed to catch (measured: `pnpm check` reported the top one unused while the assignment error vanished); (4) `displayMode` reads **position**, not `_type`, so the rendering matches the markup it lands in; blank LaTeX renders nothing, malformed LaTeX degrades to its own visible source in the theme's error token (KaTeX writes `errorColor` verbatim into a `style` attr, so a `var()` reference works and no hex is re-typed) — and the test for "KaTeX throws something `throwOnError: false` doesn't catch" found a **real** throw, since the blank-check ran outside the try and `latex` is only _typed_ as a string; (5) `{@html}` safety is **measured, not assumed** — `trust: false` refuses the markup-emitting commands, but the MathML `<annotation>` echoes the source back, so "the source never appears" is the wrong assertion and "no markup appears" is the right one; (6) the CSS import lives in the **component** so Vite scopes it to two routes, at the cost of 59 font files / 1.2 MB deployed (~900 KB of `woff`/`ttf` nothing will fetch) — accepted, because trimming to `woff2` means hand-copying upstream's 20-face `@font-face` table, and it costs a visitor nothing. → [sanity](docs/sanity.md)
- **A field the Studio ships renders nothing until a query selects it AND a page draws it (DAR-122)** — `person` has carried `fullBio`/`experience`/`education`/`focusAreas`/`responsibilities` since DAR-47, which was closed on a Studio-only PR; `seed.person.michael-harris` fills all of them (4 paragraphs, 9 positions, 3 credentials) and the site rendered ONE sentence of `bio` on a grid card with nowhere to click through to. Third instance of the same shape (DAR-73's inert `siteSettings`, DAR-76's ignored `seo.canonicalUrl`), so `docs/sanity.md`'s sync note now says a synced field lands as a TYPE and nothing more. Five things worth keeping: (1) **who has a public profile is now three surfaces** — the grid, the route, the sitemap — and a drift between them is not cosmetic: wider means advertising URLs that 404, narrower means linking from a page no crawler is told about. One interpolated `TEAM_PERSON` const (a const, never a builder function — DAR-94's `defineQuery` trap), and `queries.spec.ts` **counts** rather than contains: within those three, every `_type == "person"` filter must be the team one. Mutation-measured — a SECOND people arm added to the sitemap with its own rule passes `toContain` and fails the count. The **author** queries stay deliberately outside that set (they search the publication record, where external co-authors are 123 of the 134 author slots), and `peopleQuery` alone keeps no `defined(slug.current)` filter, because a slugless teammate belongs on the team page and merely loses their link; (2) the sitemap's real hazard is that **a content type is two halves** — a query arm and a handler mapping — and the second is silent: the query type-checks, the endpoint 200s, the document just comes back a third short. **Nothing in CI can see it**, because e2e runs without `SANITY_VIEWER_TOKEN` (DAR-96) so EVERY CMS-driven `<loc>` is absent there and `seo.e2e.ts` passes unchanged against a handler that dropped all three types — hence `sitemap.xml/server.spec.ts`, the endpoint's first test, table-driven against a mocked client (deleting the people mapping fails 2). Same reason the detail routes stay OUT of `AUDITED_PAGES` against the ticket's letter: they 404 in CI, so the audit would prove the CSP of an error page; (3) for the same token reason the **rendering** acceptance is a `client`-project chromium spec, not an e2e — a published document is just a prop there, and it is the only automated thing that says the authored fields reach the page (dropping the Education section fails it). A page rendered in isolation must `import '../layout.css'`: `CosmicBackdrop` paints from the theme's `--color-*-500` and `addColorStop('')` **throws**, which vitest reports as an unhandled error and exits 1; (4) a person is now IDENTIFIED by their profile URL — both Person nodes emit `@id` = `{origin}/people/{slug}` so index and detail are one entity, derived from the slug and never from the serving URL (a localized tree would otherwise mint one entity per locale; `mainEntityOfPage` is the served URL, because that describes the page). Writing it surfaced that `peopleJsonLd` gated social URLs on truthiness while `organizationJsonLd` used `isHttpUrl` — two identity builders disagreeing about what is publishable; (5) `personBySlugQuery` is separate from `peopleQuery` (the grid must not ship every teammate's positions) and deliberately does **not** project `person.email` — a mailbox on an indexable page is a spam surface with no way back, pinned word-anchored; (6) two crashes/defects the TESTS could not have found, both caught by looking at the built page — **keying a Sanity PRIMITIVE array by its value turns an editor's duplicate into `each_key_duplicate`, i.e. a 500 on the whole profile** (`focusAreas`/`responsibilities` have no `_key`; index-key them, and give object arrays like `socialLinks` the real `_key` instead of a `url`), and Svelte's **trimming of an `{#if}` block's leading whitespace** rendered `Ledger Rocket· 2025–Present` (the trap `PageHero` already documents, one `&nbsp;` away); (7) **asserting the request is not asserting the result** — `ogImageUrl`'s spec checked `w=1200`/`h=630` and passed for the life of the helper while the builder's default `fit=clip` returned a **504×630** portrait, because clip fits INSIDE the box; it had never mattered (no coverImage, no `seo.ogImage`, every page on the brand card), so a person's portrait was the first image it ever had to shape — and having fixed the helper, the page still doesn't use it, since a 1.91:1 crop of a headshot slices the forehead and chin off. → [sanity](docs/sanity.md) · [seo](docs/seo.md)
- **A slug is only a URL if the route can serve it, and the two checks that decide that OVERLAP (DAR-148)** — every path built from a CMS slug goes through `new URL`, which **resolves** `../`, so a `../admin` slug put `https://darcstar.tech/admin` in `<loc>` (a path `seo.e2e.ts`'s `GATED_PATHS` asserts is never there), in the team grid's `href`, and in the `Person` `@id` — an identifier pointing at the login wall. CI could see none of it: e2e runs without `SANITY_VIEWER_TOKEN` (DAR-96), so no CMS-driven `<loc>` exists there at all. Five things worth keeping: (1) **the ticket's own proposed guard was half a guard, and measuring the whole vector space is what showed it** — it checked only that the path survives a `new URL` round-trip, which passes `''` (→ `/news/`, the section index a second time), `a/b`, `/admin` and `//evil.com/x`, none of them servable by a `[slug]` route that matches ONE segment; (2) **the two halves overlap on the headline case, which is a trap for the TESTS rather than the code** — `../admin` contains a slash, so the segment check alone already refuses it, and a spec whose hostile slugs are all slash-bearing stays green with the entire round-trip deleted (measured: sitemap + all three page specs green). `..\admin` is the case that fixes it — the URL parser folds `\` to `/` in a special-scheme URL, so it escapes just as far while containing no slash — so the tables are split by **which half is exclusively responsible**, each with a paired assertion (slash-free / round-trips-cleanly) so a row can't migrate to the other half and silently take its coverage with it; (3) the guard is **`encodeURI`, never `encodeURIComponent`** — the latter escapes `&` and would drop `a&b<c`, a slug the route serves fine and the sitemap's only case exercising XML escaping: refuse the unroutable, never the merely unusual; (4) it went to **all six** places a slug becomes a URL rather than the sitemap alone, because the grid's link and the `@id` are one claim about one person (the code already SAID they shared the predicate `routable slug` and they didn't), and the posture is DAR-122's everywhere — **the card survives, the link doesn't**, with `/news` giving up the hover affordance and the "Read article" CTA along with its anchor, since a card that still says "Read article" and goes nowhere is a worse lie than the link was; (5) `GATED_PATHS` moved into `$lib/seo.ts` because two suites assert it and they **cannot share a file** (one imports `@playwright/test`, the other runs under node) — a hand-kept second copy is DAR-99's rot. Honest residual, chosen over a DAR-102-style source scan and immediately shown to be weak: the six sites are pinned and the sitemap's mapping is written once for every collection, but a **seventh** is caught by review, not by a test (filed as DAR-154) — and the sixth (`/news/[slug]`'s related-papers row, carrying the same truthiness check the ticket exists to replace) was MISSED by this change's own first sweep, because the `grep` proving "exactly five" was truncated by a `head`. A sweep that establishes a set has to be read whole, and the posture there is the one exception to card-survives-link-doesn't: a row that is nothing but a cross-reference drops entirely, since the paper still has its own card. → [seo](docs/seo.md) · [sanity](docs/sanity.md)
- **Svelte trims whitespace at a block boundary, and one `&nbsp;` is half a fix when the block owns BOTH edges (DAR-153)** — `/research` and `/research/[slug]` served `Zenodo·February 4, 2026` on all 18 cards and every paper page for the life of the index, because the `·` sat alone on its own line inside an `{#if}` and the compiler ate the space on each side. Five things worth keeping: (1) the ticket prescribed `/people/[slug]`'s **single leading `&nbsp;`** and that is measurably half a fix here — it renders `Zenodo ·February 4, 2026`, the half that looks deliberate — because the sibling surfaces' trailing space is _interior_ to their block and survives, while here it is the block's own trailing whitespace; entities are not ASCII whitespace, so **both** survive a re-wrap, proven by re-applying the exact formatting that caused the bug and watching the specs stay green rather than by asserting it; (2) the two call sites were **byte-identical**, so the fix is one `PaperVenueDate` in the `Paper*` meta family — one spec instead of two, and no third surface to get wrong; (3) writing that spec surfaced an **inherited** defect: the separator gated on the raw `publishedDate` while the text came from `formatDate`, which returns `''` for an unparseable value, so a write bypassing the Studio's date widget (DAR-70's rule) rendered a dangling `Zenodo ·` — gate on what RENDERS, and emit **no element** rather than an empty `<span>` that would still be a flex item claiming a `gap-3` (DAR-56); (4) the ticket's scope table named four surfaces and declared `/research` "the last one" while **missing `/research/[slug]`**, because it was assembled by checking the surfaces I thought of — enumerating the **pattern in source** (`grep -rn '·' src --include=*.svelte`) found it immediately, and that is the durable move, the same lesson DAR-148's truncated sweep taught one ticket earlier; (5) nothing on the site catches this by rendering — the identical trap in `/news/[slug]` survived indefinitely because no published post has related papers — so each surface carries a `client` spec that **normalises whitespace before comparing**, since `\s` matches U+00A0: the claim is "there IS separation", not the mechanism, and a plain space would still go red at the next re-wrap. → [sanity](docs/sanity.md)
