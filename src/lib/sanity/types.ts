/**
 * ---------------------------------------------------------------------------------
 * This file has been generated by Sanity TypeGen.
 * Command: `sanity typegen generate`
 *
 * Any modifications made directly to this file will be overwritten the next time
 * the TypeScript definitions are generated. Please make changes to the Sanity
 * schema definitions and/or GROQ queries if you need to update these types.
 *
 * For more information on how to use Sanity TypeGen, visit the official documentation:
 * https://www.sanity.io/docs/sanity-typegen
 * ---------------------------------------------------------------------------------
 */

export declare const internalGroqTypeReferenceTo: unique symbol;

// Source: src/lib/sanity/schema.json
export type SanityImageAssetReference = {
	_ref: string;
	_type: 'reference';
	_weak?: boolean;
	[internalGroqTypeReferenceTo]?: 'sanity.imageAsset';
};

export type Seo = {
	_type: 'seo';
	metaTitle?: string;
	metaDescription?: string;
	ogImage?: {
		asset?: SanityImageAssetReference;
		media?: unknown;
		hotspot?: SanityImageHotspot;
		crop?: SanityImageCrop;
		_type: 'image';
	};
	canonicalUrl?: string;
	noIndex?: boolean;
};

export type MathBlock = {
	_type: 'mathBlock';
	latex: string;
};

export type MathInline = {
	_type: 'mathInline';
	latex: string;
};

export type BlockContent = Array<
	| {
			children?: Array<
				| {
						marks?: Array<string>;
						text?: string;
						_type: 'span';
						_key: string;
				  }
				| ({
						_key: string;
				  } & MathInline)
			>;
			style?: 'normal' | 'h2' | 'h3' | 'h4' | 'blockquote';
			listItem?: 'bullet' | 'number';
			markDefs?: Array<{
				href: string;
				blank?: boolean;
				_type: 'link';
				_key: string;
			}>;
			level?: number;
			_type: 'block';
			_key: string;
	  }
	| {
			asset?: SanityImageAssetReference;
			media?: unknown;
			hotspot?: SanityImageHotspot;
			crop?: SanityImageCrop;
			alt: string;
			caption?: string;
			_type: 'image';
			_key: string;
	  }
	| ({
			_key: string;
	  } & Code)
	| ({
			_key: string;
	  } & MathBlock)
>;

export type SiteSettings = {
	_id: string;
	_type: 'siteSettings';
	_createdAt: string;
	_updatedAt: string;
	_rev: string;
	title: string;
	tagline?: string;
	description?: string;
	contactEmail?: string;
	logo?: {
		asset?: SanityImageAssetReference;
		media?: unknown;
		hotspot?: SanityImageHotspot;
		crop?: SanityImageCrop;
		alt?: string;
		_type: 'image';
	};
	favicon?: {
		asset?: SanityImageAssetReference;
		media?: unknown;
		hotspot?: SanityImageHotspot;
		crop?: SanityImageCrop;
		_type: 'image';
	};
	titleTemplate?: string;
	defaultOgImage?: {
		asset?: SanityImageAssetReference;
		media?: unknown;
		hotspot?: SanityImageHotspot;
		crop?: SanityImageCrop;
		_type: 'image';
	};
	socialLinks?: Array<{
		label: string;
		url: string;
		_key: string;
	}>;
	primaryNav?: Array<{
		label: string;
		href: string;
		_key: string;
	}>;
};

export type SanityImageCrop = {
	_type: 'sanity.imageCrop';
	top: number;
	bottom: number;
	left: number;
	right: number;
};

export type SanityImageHotspot = {
	_type: 'sanity.imageHotspot';
	x: number;
	y: number;
	height: number;
	width: number;
};

export type Topic = {
	_id: string;
	_type: 'topic';
	_createdAt: string;
	_updatedAt: string;
	_rev: string;
	title: string;
	slug: Slug;
	description?: string;
};

export type Slug = {
	_type: 'slug';
	current: string;
	source?: string;
};

export type Category = {
	_id: string;
	_type: 'category';
	_createdAt: string;
	_updatedAt: string;
	_rev: string;
	title: string;
	slug: Slug;
	description?: string;
};

export type Person = {
	_id: string;
	_type: 'person';
	_createdAt: string;
	_updatedAt: string;
	_rev: string;
	name: string;
	nameSortKey?: string;
	slug: Slug;
	kind?: 'internal' | 'external';
	role?: string;
	image?: {
		asset?: SanityImageAssetReference;
		media?: unknown;
		hotspot?: SanityImageHotspot;
		crop?: SanityImageCrop;
		alt?: string;
		_type: 'image';
	};
	bio?: string;
	email?: string;
	socialLinks?: Array<{
		label: string;
		url: string;
		_key: string;
	}>;
	fullBio?: BlockContent;
	focusAreas?: Array<string>;
	responsibilities?: Array<string>;
	experience?: Array<{
		title: string;
		organization: string;
		startYear?: number;
		endYear?: number;
		summary?: string;
		url?: string;
		_type: 'position';
		_key: string;
	}>;
	education?: Array<{
		qualification: string;
		institution: string;
		year?: number;
		_type: 'credential';
		_key: string;
	}>;
};

export type PersonReference = {
	_ref: string;
	_type: 'reference';
	_weak?: boolean;
	[internalGroqTypeReferenceTo]?: 'person';
};

export type CategoryReference = {
	_ref: string;
	_type: 'reference';
	_weak?: boolean;
	[internalGroqTypeReferenceTo]?: 'category';
};

export type TopicReference = {
	_ref: string;
	_type: 'reference';
	_weak?: boolean;
	[internalGroqTypeReferenceTo]?: 'topic';
};

export type SanityFileAssetReference = {
	_ref: string;
	_type: 'reference';
	_weak?: boolean;
	[internalGroqTypeReferenceTo]?: 'sanity.fileAsset';
};

export type Paper = {
	_id: string;
	_type: 'paper';
	_createdAt: string;
	_updatedAt: string;
	_rev: string;
	title: string;
	titleSortKey?: string;
	slug: Slug;
	authors?: Array<
		{
			_key: string;
		} & PersonReference
	>;
	darcstarAuthored?: boolean;
	status?: 'draft' | 'preprint' | 'toAppear' | 'published';
	contribution?: 'conceptual' | 'formal' | 'empirical' | 'engineering';
	abstract?: string;
	commentary?: BlockContent;
	categories?: Array<
		{
			_key: string;
		} & CategoryReference
	>;
	topics?: Array<
		{
			_key: string;
		} & TopicReference
	>;
	venue?: string;
	publishedDate?: string;
	url?: string;
	doi?: string;
	arxivId?: string;
	codeUrl?: string;
	pdf?: {
		asset?: SanityFileAssetReference;
		media?: unknown;
		_type: 'file';
	};
	seo?: Seo;
};

export type SeriesReference = {
	_ref: string;
	_type: 'reference';
	_weak?: boolean;
	[internalGroqTypeReferenceTo]?: 'series';
};

export type PaperReference = {
	_ref: string;
	_type: 'reference';
	_weak?: boolean;
	[internalGroqTypeReferenceTo]?: 'paper';
};

export type Post = {
	_id: string;
	_type: 'post';
	_createdAt: string;
	_updatedAt: string;
	_rev: string;
	title: string;
	slug: Slug;
	language?: string;
	excerpt?: string;
	authors?: Array<
		{
			_key: string;
		} & PersonReference
	>;
	categories?: Array<
		{
			_key: string;
		} & CategoryReference
	>;
	series?: SeriesReference;
	seriesPart?: number;
	relatedPapers?: Array<
		{
			_key: string;
		} & PaperReference
	>;
	publishedAt: string;
	featured?: boolean;
	coverImage?: {
		asset?: SanityImageAssetReference;
		media?: unknown;
		hotspot?: SanityImageHotspot;
		crop?: SanityImageCrop;
		alt: string;
		_type: 'image';
	};
	body?: BlockContent;
	seo?: Seo;
};

export type Series = {
	_id: string;
	_type: 'series';
	_createdAt: string;
	_updatedAt: string;
	_rev: string;
	title: string;
	description?: string;
};

export type MediaTag = {
	_id: string;
	_type: 'media.tag';
	_createdAt: string;
	_updatedAt: string;
	_rev: string;
	name?: Slug;
};

export type Code = {
	_type: 'code';
	language?: string;
	filename?: string;
	code?: string;
	highlightedLines?: Array<number>;
};

export type SanityImagePaletteSwatch = {
	_type: 'sanity.imagePaletteSwatch';
	background?: string;
	foreground?: string;
	population?: number;
	title?: string;
};

export type SanityImagePalette = {
	_type: 'sanity.imagePalette';
	darkMuted?: SanityImagePaletteSwatch;
	lightVibrant?: SanityImagePaletteSwatch;
	darkVibrant?: SanityImagePaletteSwatch;
	vibrant?: SanityImagePaletteSwatch;
	dominant?: SanityImagePaletteSwatch;
	lightMuted?: SanityImagePaletteSwatch;
	muted?: SanityImagePaletteSwatch;
};

export type SanityImageDimensions = {
	_type: 'sanity.imageDimensions';
	height: number;
	width: number;
	aspectRatio: number;
};

export type SanityImageMetadata = {
	_type: 'sanity.imageMetadata';
	location?: Geopoint;
	dimensions?: SanityImageDimensions;
	palette?: SanityImagePalette;
	lqip?: string;
	blurHash?: string;
	thumbHash?: string;
	hasAlpha?: boolean;
	isOpaque?: boolean;
};

export type SanityFileAsset = {
	_id: string;
	_type: 'sanity.fileAsset';
	_createdAt: string;
	_updatedAt: string;
	_rev: string;
	originalFilename?: string;
	label?: string;
	title?: string;
	description?: string;
	altText?: string;
	sha1hash: string;
	extension: string;
	mimeType: string;
	size: number;
	assetId: string;
	uploadId?: string;
	path: string;
	url: string;
	source?: SanityAssetSourceData;
};

export type SanityAssetSourceData = {
	_type: 'sanity.assetSourceData';
	name?: string;
	id?: string;
	url?: string;
};

export type SanityImageAsset = {
	_id: string;
	_type: 'sanity.imageAsset';
	_createdAt: string;
	_updatedAt: string;
	_rev: string;
	originalFilename?: string;
	label?: string;
	title?: string;
	description?: string;
	altText?: string;
	sha1hash: string;
	extension: string;
	mimeType: string;
	size: number;
	assetId: string;
	uploadId?: string;
	path: string;
	url: string;
	metadata?: SanityImageMetadata;
	source?: SanityAssetSourceData;
};

export type Geopoint = {
	_type: 'geopoint';
	lat?: number;
	lng?: number;
	alt?: number;
};

export type AllSanitySchemaTypes =
	| SanityImageAssetReference
	| Seo
	| MathBlock
	| MathInline
	| BlockContent
	| SiteSettings
	| SanityImageCrop
	| SanityImageHotspot
	| Topic
	| Slug
	| Category
	| Person
	| PersonReference
	| CategoryReference
	| TopicReference
	| SanityFileAssetReference
	| Paper
	| SeriesReference
	| PaperReference
	| Post
	| Series
	| MediaTag
	| Code
	| SanityImagePaletteSwatch
	| SanityImagePalette
	| SanityImageDimensions
	| SanityImageMetadata
	| SanityFileAsset
	| SanityAssetSourceData
	| SanityImageAsset
	| Geopoint;

// Source: src/lib/sanity/queries.ts
// Variable: papersPageByDateQuery
// Query: {		"papers": *[_type == "paper" && defined(slug.current)		&& ($topic == null || $topic in topics[]->slug.current)		&& ($contribution == null || contribution == $contribution)		&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))		&& ($origin == null			|| ($origin == "darcstar" && darcstarAuthored == true)			|| ($origin == "external" && darcstarAuthored != true))] | order(select(darcstarAuthored == true => 0, 1) asc, publishedDate desc) [$offset...$end] {			_id,			title,			"slug": slug.current,			status,			contribution,			darcstarAuthored,			"hasCommentary": coalesce(count(commentary) > 0, false),			venue,			publishedDate,			url,			doi,			arxivId,			codeUrl,			"abstract": select(count(string::split(abstract, " ")) > 50 => array::join(string::split(abstract, " ")[0...50], " ") + "…", abstract),			"authors": array::compact(authors[]->{ _id, name, "slug": slug.current }),			"topics": array::compact(topics[]->{ _id, title, "slug": slug.current })		},		"total": count(*[_type == "paper" && defined(slug.current)		&& ($topic == null || $topic in topics[]->slug.current)		&& ($contribution == null || contribution == $contribution)		&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))		&& ($origin == null			|| ($origin == "darcstar" && darcstarAuthored == true)			|| ($origin == "external" && darcstarAuthored != true))]),		"totalAll": count(*[_type == "paper" && defined(slug.current)]),		"topics": *[_type == "topic" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(title asc) {			"slug": slug.current,			title,			description		},		"contributions": array::unique(*[_type == "paper" && defined(slug.current) && defined(contribution)].contribution),		"teamAuthors": *[_type == "person" && kind != "external" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(coalesce(nameSortKey, lower(name)) asc) {			"value": slug.current,			"label": name,			"key": nameSortKey		},		"authorLabel": *[_type == "person" && defined(slug.current) && slug.current == $author][0].name	}
export type PapersPageByDateQueryResult = {
	papers: Array<{
		_id: string;
		title: string;
		slug: string;
		status: 'draft' | 'preprint' | 'published' | 'toAppear' | null;
		contribution: 'conceptual' | 'empirical' | 'engineering' | 'formal' | null;
		darcstarAuthored: boolean | null;
		hasCommentary: boolean | false;
		venue: string | null;
		publishedDate: string | null;
		url: string | null;
		doi: string | null;
		arxivId: string | null;
		codeUrl: string | null;
		abstract: string | null;
		authors: Array<{
			_id: string;
			name: string;
			slug: string;
		}> | null;
		topics: Array<{
			_id: string;
			title: string;
			slug: string;
		}> | null;
	}>;
	total: number;
	totalAll: number;
	topics: Array<{
		slug: string;
		title: string;
		description: string | null;
	}>;
	contributions: Array<'conceptual' | 'empirical' | 'engineering' | 'formal'>;
	teamAuthors: Array<{
		value: string;
		label: string;
		key: string | null;
	}>;
	authorLabel: string | null;
};

// Source: src/lib/sanity/queries.ts
// Variable: papersPageByDateAscQuery
// Query: {		"papers": *[_type == "paper" && defined(slug.current)		&& ($topic == null || $topic in topics[]->slug.current)		&& ($contribution == null || contribution == $contribution)		&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))		&& ($origin == null			|| ($origin == "darcstar" && darcstarAuthored == true)			|| ($origin == "external" && darcstarAuthored != true))] | order(select(darcstarAuthored == true => 0, 1) asc, coalesce(publishedDate, "9999-12-31") asc) [$offset...$end] {			_id,			title,			"slug": slug.current,			status,			contribution,			darcstarAuthored,			"hasCommentary": coalesce(count(commentary) > 0, false),			venue,			publishedDate,			url,			doi,			arxivId,			codeUrl,			"abstract": select(count(string::split(abstract, " ")) > 50 => array::join(string::split(abstract, " ")[0...50], " ") + "…", abstract),			"authors": array::compact(authors[]->{ _id, name, "slug": slug.current }),			"topics": array::compact(topics[]->{ _id, title, "slug": slug.current })		},		"total": count(*[_type == "paper" && defined(slug.current)		&& ($topic == null || $topic in topics[]->slug.current)		&& ($contribution == null || contribution == $contribution)		&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))		&& ($origin == null			|| ($origin == "darcstar" && darcstarAuthored == true)			|| ($origin == "external" && darcstarAuthored != true))]),		"totalAll": count(*[_type == "paper" && defined(slug.current)]),		"topics": *[_type == "topic" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(title asc) {			"slug": slug.current,			title,			description		},		"contributions": array::unique(*[_type == "paper" && defined(slug.current) && defined(contribution)].contribution),		"teamAuthors": *[_type == "person" && kind != "external" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(coalesce(nameSortKey, lower(name)) asc) {			"value": slug.current,			"label": name,			"key": nameSortKey		},		"authorLabel": *[_type == "person" && defined(slug.current) && slug.current == $author][0].name	}
export type PapersPageByDateAscQueryResult = {
	papers: Array<{
		_id: string;
		title: string;
		slug: string;
		status: 'draft' | 'preprint' | 'published' | 'toAppear' | null;
		contribution: 'conceptual' | 'empirical' | 'engineering' | 'formal' | null;
		darcstarAuthored: boolean | null;
		hasCommentary: boolean | false;
		venue: string | null;
		publishedDate: string | null;
		url: string | null;
		doi: string | null;
		arxivId: string | null;
		codeUrl: string | null;
		abstract: string | null;
		authors: Array<{
			_id: string;
			name: string;
			slug: string;
		}> | null;
		topics: Array<{
			_id: string;
			title: string;
			slug: string;
		}> | null;
	}>;
	total: number;
	totalAll: number;
	topics: Array<{
		slug: string;
		title: string;
		description: string | null;
	}>;
	contributions: Array<'conceptual' | 'empirical' | 'engineering' | 'formal'>;
	teamAuthors: Array<{
		value: string;
		label: string;
		key: string | null;
	}>;
	authorLabel: string | null;
};

// Source: src/lib/sanity/queries.ts
// Variable: papersPageByTitleQuery
// Query: {		"papers": *[_type == "paper" && defined(slug.current)		&& ($topic == null || $topic in topics[]->slug.current)		&& ($contribution == null || contribution == $contribution)		&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))		&& ($origin == null			|| ($origin == "darcstar" && darcstarAuthored == true)			|| ($origin == "external" && darcstarAuthored != true))] | order(coalesce(titleSortKey, lower(title)) asc) [$offset...$end] {			_id,			title,			"slug": slug.current,			status,			contribution,			darcstarAuthored,			"hasCommentary": coalesce(count(commentary) > 0, false),			venue,			publishedDate,			url,			doi,			arxivId,			codeUrl,			"abstract": select(count(string::split(abstract, " ")) > 50 => array::join(string::split(abstract, " ")[0...50], " ") + "…", abstract),			"authors": array::compact(authors[]->{ _id, name, "slug": slug.current }),			"topics": array::compact(topics[]->{ _id, title, "slug": slug.current })		},		"total": count(*[_type == "paper" && defined(slug.current)		&& ($topic == null || $topic in topics[]->slug.current)		&& ($contribution == null || contribution == $contribution)		&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))		&& ($origin == null			|| ($origin == "darcstar" && darcstarAuthored == true)			|| ($origin == "external" && darcstarAuthored != true))]),		"totalAll": count(*[_type == "paper" && defined(slug.current)]),		"topics": *[_type == "topic" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(title asc) {			"slug": slug.current,			title,			description		},		"contributions": array::unique(*[_type == "paper" && defined(slug.current) && defined(contribution)].contribution),		"teamAuthors": *[_type == "person" && kind != "external" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(coalesce(nameSortKey, lower(name)) asc) {			"value": slug.current,			"label": name,			"key": nameSortKey		},		"authorLabel": *[_type == "person" && defined(slug.current) && slug.current == $author][0].name	}
export type PapersPageByTitleQueryResult = {
	papers: Array<{
		_id: string;
		title: string;
		slug: string;
		status: 'draft' | 'preprint' | 'published' | 'toAppear' | null;
		contribution: 'conceptual' | 'empirical' | 'engineering' | 'formal' | null;
		darcstarAuthored: boolean | null;
		hasCommentary: boolean | false;
		venue: string | null;
		publishedDate: string | null;
		url: string | null;
		doi: string | null;
		arxivId: string | null;
		codeUrl: string | null;
		abstract: string | null;
		authors: Array<{
			_id: string;
			name: string;
			slug: string;
		}> | null;
		topics: Array<{
			_id: string;
			title: string;
			slug: string;
		}> | null;
	}>;
	total: number;
	totalAll: number;
	topics: Array<{
		slug: string;
		title: string;
		description: string | null;
	}>;
	contributions: Array<'conceptual' | 'empirical' | 'engineering' | 'formal'>;
	teamAuthors: Array<{
		value: string;
		label: string;
		key: string | null;
	}>;
	authorLabel: string | null;
};

// Source: src/lib/sanity/queries.ts
// Variable: authorSuggestionsQuery
// Query: *[_type == "person" && defined(slug.current) && (name match ($q + "*") || nameSortKey match ($q + "*")) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0]		| order(select(kind != "external" => 0, 1) asc, coalesce(nameSortKey, lower(name)) asc) [0...12] {			"value": slug.current,			"label": name,			"key": nameSortKey		}
export type AuthorSuggestionsQueryResult = Array<{
	value: string;
	label: string;
	key: string | null;
}>;

// Source: src/lib/sanity/queries.ts
// Variable: postsPageQuery
// Query: {		"posts": *[_type == "post" && defined(slug.current)] | order(publishedAt desc) [$offset...$end] {			_id,			title,			"slug": slug.current,			excerpt,			publishedAt,			coverImage,			"authors": array::compact(authors[]->{ _id, name, "slug": slug.current, role })		},		"total": count(*[_type == "post" && defined(slug.current)])	}
export type PostsPageQueryResult = {
	posts: Array<{
		_id: string;
		title: string;
		slug: string;
		excerpt: string | null;
		publishedAt: string;
		coverImage: {
			asset?: SanityImageAssetReference;
			media?: unknown;
			hotspot?: SanityImageHotspot;
			crop?: SanityImageCrop;
			alt: string;
			_type: 'image';
		} | null;
		authors: Array<{
			_id: string;
			name: string;
			slug: string;
			role: string | null;
		}> | null;
	}>;
	total: number;
};

// Source: src/lib/sanity/queries.ts
// Variable: postBySlugQuery
// Query: *[_type == "post" && slug.current == $slug][0] {		_id,		_updatedAt,		title,		"slug": slug.current,		excerpt,		publishedAt,		coverImage,		body,		"authors": array::compact(authors[]->{ _id, name, "slug": slug.current, role, image }),		"categories": array::compact(categories[]->{ _id, title, "slug": slug.current }),		"relatedPapers": array::compact(relatedPapers[]->{ _id, title, "slug": slug.current, venue, darcstarAuthored, "hasCommentary": coalesce(count(commentary) > 0, false) }),		seo	}
export type PostBySlugQueryResult = {
	_id: string;
	_updatedAt: string;
	title: string;
	slug: string;
	excerpt: string | null;
	publishedAt: string;
	coverImage: {
		asset?: SanityImageAssetReference;
		media?: unknown;
		hotspot?: SanityImageHotspot;
		crop?: SanityImageCrop;
		alt: string;
		_type: 'image';
	} | null;
	body: BlockContent | null;
	authors: Array<{
		_id: string;
		name: string;
		slug: string;
		role: string | null;
		image: {
			asset?: SanityImageAssetReference;
			media?: unknown;
			hotspot?: SanityImageHotspot;
			crop?: SanityImageCrop;
			alt?: string;
			_type: 'image';
		} | null;
	}> | null;
	categories: Array<{
		_id: string;
		title: string;
		slug: string;
	}> | null;
	relatedPapers: Array<{
		_id: string;
		title: string;
		slug: string;
		venue: string | null;
		darcstarAuthored: boolean | null;
		hasCommentary: boolean | false;
	}> | null;
	seo: Seo | null;
} | null;

// Source: src/lib/sanity/queries.ts
// Variable: paperBySlugQuery
// Query: *[_type == "paper" && slug.current == $slug][0] {		_id,		_updatedAt,		title,		"slug": slug.current,		status,		contribution,		darcstarAuthored,		abstract,		commentary,		venue,		publishedDate,		url,		doi,		arxivId,		codeUrl,		"pdfUrl": pdf.asset->url,		"authors": array::compact(authors[]->{ _id, name, "slug": slug.current, role }),		"topics": array::compact(topics[]->{ _id, title, "slug": slug.current, description }),		"categories": array::compact(categories[]->{ _id, title, "slug": slug.current }),		seo	}
export type PaperBySlugQueryResult = {
	_id: string;
	_updatedAt: string;
	title: string;
	slug: string;
	status: 'draft' | 'preprint' | 'published' | 'toAppear' | null;
	contribution: 'conceptual' | 'empirical' | 'engineering' | 'formal' | null;
	darcstarAuthored: boolean | null;
	abstract: string | null;
	commentary: BlockContent | null;
	venue: string | null;
	publishedDate: string | null;
	url: string | null;
	doi: string | null;
	arxivId: string | null;
	codeUrl: string | null;
	pdfUrl: string | null;
	authors: Array<{
		_id: string;
		name: string;
		slug: string;
		role: string | null;
	}> | null;
	topics: Array<{
		_id: string;
		title: string;
		slug: string;
		description: string | null;
	}> | null;
	categories: Array<{
		_id: string;
		title: string;
		slug: string;
	}> | null;
	seo: Seo | null;
} | null;

// Source: src/lib/sanity/queries.ts
// Variable: sitemapEntriesQuery
// Query: {	"posts": *[_type == "post" && defined(slug.current) && seo.noIndex != true]{ "slug": slug.current, _updatedAt },	"papers": *[_type == "paper" && defined(slug.current) && seo.noIndex != true]{ "slug": slug.current, _updatedAt },	"people": *[_type == "person" && kind != "external" && defined(slug.current)]{ "slug": slug.current, _updatedAt }}
export type SitemapEntriesQueryResult = {
	posts: Array<{
		slug: string;
		_updatedAt: string;
	}>;
	papers: Array<{
		slug: string;
		_updatedAt: string;
	}>;
	people: Array<{
		slug: string;
		_updatedAt: string;
	}>;
};

// Source: src/lib/sanity/queries.ts
// Variable: siteSettingsQuery
// Query: *[_id == "siteSettings"][0] {		socialLinks[]{ label, url }	}
export type SiteSettingsQueryResult =
	| {
			socialLinks: null;
	  }
	| {
			socialLinks: Array<{
				label: string;
				url: string;
			}> | null;
	  }
	| null;

// Source: src/lib/sanity/queries.ts
// Variable: peopleQuery
// Query: *[_type == "person" && kind != "external"] | order(coalesce(nameSortKey, lower(name)) asc) {		_id,		name,		"slug": slug.current,		role,		image,		bio,		socialLinks[]{ _key, label, url }	}
export type PeopleQueryResult = Array<{
	_id: string;
	name: string;
	slug: string;
	role: string | null;
	image: {
		asset?: SanityImageAssetReference;
		media?: unknown;
		hotspot?: SanityImageHotspot;
		crop?: SanityImageCrop;
		alt?: string;
		_type: 'image';
	} | null;
	bio: string | null;
	socialLinks: Array<{
		_key: string;
		label: string;
		url: string;
	}> | null;
}>;

// Source: src/lib/sanity/queries.ts
// Variable: personBySlugQuery
// Query: *[_type == "person" && kind != "external" && slug.current == $slug][0] {		_id,		_updatedAt,		name,		"slug": slug.current,		role,		image,		bio,		fullBio,		focusAreas,		responsibilities,		experience[]{ _key, title, organization, startYear, endYear, summary, url },		education[]{ _key, qualification, institution, year },		socialLinks[]{ _key, label, url }	}
export type PersonBySlugQueryResult = {
	_id: string;
	_updatedAt: string;
	name: string;
	slug: string;
	role: string | null;
	image: {
		asset?: SanityImageAssetReference;
		media?: unknown;
		hotspot?: SanityImageHotspot;
		crop?: SanityImageCrop;
		alt?: string;
		_type: 'image';
	} | null;
	bio: string | null;
	fullBio: BlockContent | null;
	focusAreas: Array<string> | null;
	responsibilities: Array<string> | null;
	experience: Array<{
		_key: string;
		title: string;
		organization: string;
		startYear: number | null;
		endYear: number | null;
		summary: string | null;
		url: string | null;
	}> | null;
	education: Array<{
		_key: string;
		qualification: string;
		institution: string;
		year: number | null;
	}> | null;
	socialLinks: Array<{
		_key: string;
		label: string;
		url: string;
	}> | null;
} | null;

// Query TypeMap
import '@sanity/client';
declare module '@sanity/client' {
	interface SanityQueries {
		'{\n\t\t"papers": *[_type == "paper" && defined(slug.current)\n\t\t&& ($topic == null || $topic in topics[]->slug.current)\n\t\t&& ($contribution == null || contribution == $contribution)\n\t\t&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))\n\t\t&& ($origin == null\n\t\t\t|| ($origin == "darcstar" && darcstarAuthored == true)\n\t\t\t|| ($origin == "external" && darcstarAuthored != true))] | order(select(darcstarAuthored == true => 0, 1) asc, publishedDate desc) [$offset...$end] {\n\t\t\t_id,\n\t\t\ttitle,\n\t\t\t"slug": slug.current,\n\t\t\tstatus,\n\t\t\tcontribution,\n\t\t\tdarcstarAuthored,\n\t\t\t"hasCommentary": coalesce(count(commentary) > 0, false),\n\t\t\tvenue,\n\t\t\tpublishedDate,\n\t\t\turl,\n\t\t\tdoi,\n\t\t\tarxivId,\n\t\t\tcodeUrl,\n\t\t\t"abstract": select(count(string::split(abstract, " ")) > 50 => array::join(string::split(abstract, " ")[0...50], " ") + "\u2026", abstract),\n\t\t\t"authors": array::compact(authors[]->{ _id, name, "slug": slug.current }),\n\t\t\t"topics": array::compact(topics[]->{ _id, title, "slug": slug.current })\n\t\t},\n\t\t"total": count(*[_type == "paper" && defined(slug.current)\n\t\t&& ($topic == null || $topic in topics[]->slug.current)\n\t\t&& ($contribution == null || contribution == $contribution)\n\t\t&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))\n\t\t&& ($origin == null\n\t\t\t|| ($origin == "darcstar" && darcstarAuthored == true)\n\t\t\t|| ($origin == "external" && darcstarAuthored != true))]),\n\t\t"totalAll": count(*[_type == "paper" && defined(slug.current)]),\n\t\t"topics": *[_type == "topic" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(title asc) {\n\t\t\t"slug": slug.current,\n\t\t\ttitle,\n\t\t\tdescription\n\t\t},\n\t\t"contributions": array::unique(*[_type == "paper" && defined(slug.current) && defined(contribution)].contribution),\n\t\t"teamAuthors": *[_type == "person" && kind != "external" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(coalesce(nameSortKey, lower(name)) asc) {\n\t\t\t"value": slug.current,\n\t\t\t"label": name,\n\t\t\t"key": nameSortKey\n\t\t},\n\t\t"authorLabel": *[_type == "person" && defined(slug.current) && slug.current == $author][0].name\n\t}': PapersPageByDateQueryResult;
		'{\n\t\t"papers": *[_type == "paper" && defined(slug.current)\n\t\t&& ($topic == null || $topic in topics[]->slug.current)\n\t\t&& ($contribution == null || contribution == $contribution)\n\t\t&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))\n\t\t&& ($origin == null\n\t\t\t|| ($origin == "darcstar" && darcstarAuthored == true)\n\t\t\t|| ($origin == "external" && darcstarAuthored != true))] | order(select(darcstarAuthored == true => 0, 1) asc, coalesce(publishedDate, "9999-12-31") asc) [$offset...$end] {\n\t\t\t_id,\n\t\t\ttitle,\n\t\t\t"slug": slug.current,\n\t\t\tstatus,\n\t\t\tcontribution,\n\t\t\tdarcstarAuthored,\n\t\t\t"hasCommentary": coalesce(count(commentary) > 0, false),\n\t\t\tvenue,\n\t\t\tpublishedDate,\n\t\t\turl,\n\t\t\tdoi,\n\t\t\tarxivId,\n\t\t\tcodeUrl,\n\t\t\t"abstract": select(count(string::split(abstract, " ")) > 50 => array::join(string::split(abstract, " ")[0...50], " ") + "\u2026", abstract),\n\t\t\t"authors": array::compact(authors[]->{ _id, name, "slug": slug.current }),\n\t\t\t"topics": array::compact(topics[]->{ _id, title, "slug": slug.current })\n\t\t},\n\t\t"total": count(*[_type == "paper" && defined(slug.current)\n\t\t&& ($topic == null || $topic in topics[]->slug.current)\n\t\t&& ($contribution == null || contribution == $contribution)\n\t\t&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))\n\t\t&& ($origin == null\n\t\t\t|| ($origin == "darcstar" && darcstarAuthored == true)\n\t\t\t|| ($origin == "external" && darcstarAuthored != true))]),\n\t\t"totalAll": count(*[_type == "paper" && defined(slug.current)]),\n\t\t"topics": *[_type == "topic" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(title asc) {\n\t\t\t"slug": slug.current,\n\t\t\ttitle,\n\t\t\tdescription\n\t\t},\n\t\t"contributions": array::unique(*[_type == "paper" && defined(slug.current) && defined(contribution)].contribution),\n\t\t"teamAuthors": *[_type == "person" && kind != "external" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(coalesce(nameSortKey, lower(name)) asc) {\n\t\t\t"value": slug.current,\n\t\t\t"label": name,\n\t\t\t"key": nameSortKey\n\t\t},\n\t\t"authorLabel": *[_type == "person" && defined(slug.current) && slug.current == $author][0].name\n\t}': PapersPageByDateAscQueryResult;
		'{\n\t\t"papers": *[_type == "paper" && defined(slug.current)\n\t\t&& ($topic == null || $topic in topics[]->slug.current)\n\t\t&& ($contribution == null || contribution == $contribution)\n\t\t&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))\n\t\t&& ($origin == null\n\t\t\t|| ($origin == "darcstar" && darcstarAuthored == true)\n\t\t\t|| ($origin == "external" && darcstarAuthored != true))] | order(coalesce(titleSortKey, lower(title)) asc) [$offset...$end] {\n\t\t\t_id,\n\t\t\ttitle,\n\t\t\t"slug": slug.current,\n\t\t\tstatus,\n\t\t\tcontribution,\n\t\t\tdarcstarAuthored,\n\t\t\t"hasCommentary": coalesce(count(commentary) > 0, false),\n\t\t\tvenue,\n\t\t\tpublishedDate,\n\t\t\turl,\n\t\t\tdoi,\n\t\t\tarxivId,\n\t\t\tcodeUrl,\n\t\t\t"abstract": select(count(string::split(abstract, " ")) > 50 => array::join(string::split(abstract, " ")[0...50], " ") + "\u2026", abstract),\n\t\t\t"authors": array::compact(authors[]->{ _id, name, "slug": slug.current }),\n\t\t\t"topics": array::compact(topics[]->{ _id, title, "slug": slug.current })\n\t\t},\n\t\t"total": count(*[_type == "paper" && defined(slug.current)\n\t\t&& ($topic == null || $topic in topics[]->slug.current)\n\t\t&& ($contribution == null || contribution == $contribution)\n\t\t&& ($author == null || $author in authors[]->slug.current || authors[]->name match ($author + "*") || authors[]->nameSortKey match ($author + "*"))\n\t\t&& ($origin == null\n\t\t\t|| ($origin == "darcstar" && darcstarAuthored == true)\n\t\t\t|| ($origin == "external" && darcstarAuthored != true))]),\n\t\t"totalAll": count(*[_type == "paper" && defined(slug.current)]),\n\t\t"topics": *[_type == "topic" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(title asc) {\n\t\t\t"slug": slug.current,\n\t\t\ttitle,\n\t\t\tdescription\n\t\t},\n\t\t"contributions": array::unique(*[_type == "paper" && defined(slug.current) && defined(contribution)].contribution),\n\t\t"teamAuthors": *[_type == "person" && kind != "external" && defined(slug.current) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0] | order(coalesce(nameSortKey, lower(name)) asc) {\n\t\t\t"value": slug.current,\n\t\t\t"label": name,\n\t\t\t"key": nameSortKey\n\t\t},\n\t\t"authorLabel": *[_type == "person" && defined(slug.current) && slug.current == $author][0].name\n\t}': PapersPageByTitleQueryResult;
		'\n\t*[_type == "person" && defined(slug.current) && (name match ($q + "*") || nameSortKey match ($q + "*")) && count(*[_type == "paper" && defined(slug.current) && references(^._id)]) > 0]\n\t\t| order(select(kind != "external" => 0, 1) asc, coalesce(nameSortKey, lower(name)) asc) [0...12] {\n\t\t\t"value": slug.current,\n\t\t\t"label": name,\n\t\t\t"key": nameSortKey\n\t\t}\n': AuthorSuggestionsQueryResult;
		'{\n\t\t"posts": *[_type == "post" && defined(slug.current)] | order(publishedAt desc) [$offset...$end] {\n\t\t\t_id,\n\t\t\ttitle,\n\t\t\t"slug": slug.current,\n\t\t\texcerpt,\n\t\t\tpublishedAt,\n\t\t\tcoverImage,\n\t\t\t"authors": array::compact(authors[]->{ _id, name, "slug": slug.current, role })\n\t\t},\n\t\t"total": count(*[_type == "post" && defined(slug.current)])\n\t}': PostsPageQueryResult;
		'\n\t*[_type == "post" && slug.current == $slug][0] {\n\t\t_id,\n\t\t_updatedAt,\n\t\ttitle,\n\t\t"slug": slug.current,\n\t\texcerpt,\n\t\tpublishedAt,\n\t\tcoverImage,\n\t\tbody,\n\t\t"authors": array::compact(authors[]->{ _id, name, "slug": slug.current, role, image }),\n\t\t"categories": array::compact(categories[]->{ _id, title, "slug": slug.current }),\n\t\t"relatedPapers": array::compact(relatedPapers[]->{ _id, title, "slug": slug.current, venue, darcstarAuthored, "hasCommentary": coalesce(count(commentary) > 0, false) }),\n\t\tseo\n\t}\n': PostBySlugQueryResult;
		'\n\t*[_type == "paper" && slug.current == $slug][0] {\n\t\t_id,\n\t\t_updatedAt,\n\t\ttitle,\n\t\t"slug": slug.current,\n\t\tstatus,\n\t\tcontribution,\n\t\tdarcstarAuthored,\n\t\tabstract,\n\t\tcommentary,\n\t\tvenue,\n\t\tpublishedDate,\n\t\turl,\n\t\tdoi,\n\t\tarxivId,\n\t\tcodeUrl,\n\t\t"pdfUrl": pdf.asset->url,\n\t\t"authors": array::compact(authors[]->{ _id, name, "slug": slug.current, role }),\n\t\t"topics": array::compact(topics[]->{ _id, title, "slug": slug.current, description }),\n\t\t"categories": array::compact(categories[]->{ _id, title, "slug": slug.current }),\n\t\tseo\n\t}\n': PaperBySlugQueryResult;
		'{\n\t"posts": *[_type == "post" && defined(slug.current) && seo.noIndex != true]{ "slug": slug.current, _updatedAt },\n\t"papers": *[_type == "paper" && defined(slug.current) && seo.noIndex != true]{ "slug": slug.current, _updatedAt },\n\t"people": *[_type == "person" && kind != "external" && defined(slug.current)]{ "slug": slug.current, _updatedAt }\n}': SitemapEntriesQueryResult;
		'\n\t*[_id == "siteSettings"][0] {\n\t\tsocialLinks[]{ label, url }\n\t}\n': SiteSettingsQueryResult;
		'\n\t*[_type == "person" && kind != "external"] | order(coalesce(nameSortKey, lower(name)) asc) {\n\t\t_id,\n\t\tname,\n\t\t"slug": slug.current,\n\t\trole,\n\t\timage,\n\t\tbio,\n\t\tsocialLinks[]{ _key, label, url }\n\t}\n': PeopleQueryResult;
		'\n\t*[_type == "person" && kind != "external" && slug.current == $slug][0] {\n\t\t_id,\n\t\t_updatedAt,\n\t\tname,\n\t\t"slug": slug.current,\n\t\trole,\n\t\timage,\n\t\tbio,\n\t\tfullBio,\n\t\tfocusAreas,\n\t\tresponsibilities,\n\t\texperience[]{ _key, title, organization, startYear, endYear, summary, url },\n\t\teducation[]{ _key, qualification, institution, year },\n\t\tsocialLinks[]{ _key, label, url }\n\t}\n': PersonBySlugQueryResult;
	}
}
